// Kept as a subpath export (`@usertrack/better-auth/protocol`) for 0.1.x consumers; the protocol now lives in @usertrack/protocol.
export * from "@usertrack/protocol";
export const PROVIDER_ID = "better-auth" as const;
