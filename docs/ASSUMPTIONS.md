# Assumptions Log

Decisions made autonomously during the CodeCraft loop. Each entry: what was assumed, why, and how to revisit.

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A1 | **Next.js 16 App Router** (not Vite/CRA). | Metadata API, `next/og` ImageResponse, RSC + Convex `fetchQuery` for SEO pages. | n/a |
| A2 | **Auth data lives inside Convex** via `@convex-dev/better-auth` component. | Single backend, no external Postgres, Better Auth remains the auth engine. Docs pin `better-auth ~1.6.x`; `better-auth` is **pinned to exactly 1.6.15**: 1.6.30 breaks `ConvexBetterAuthProvider`'s `AuthClient` typing (`useSession().data: never`). pnpm uses `node-linker=hoisted` (`.npmrc`) so only one copy of `better-auth` types exists. | If the component blocks, fall back to Better Auth + Railway Postgres and mirror user ids into Convex. |
| A3 | **Ranking formula** = verified net new users in trailing 30 days (`total_now − total_30d_ago`), tiebreak by 30d growth %. | Simple, explainable, growth-first, not size-first. | Could add a blended score later. |
| A4 | **Trust levels**: `verified` (auto-synced from a 3rd-party auth provider or a domain-matched endpoint), `unverified` (manual/self-reported or non-domain-matched endpoint), `pending` (connected, no successful sync yet). | Matches brief; keeps manual entry possible but never ranked as verified. | Add stronger verification (DNS TXT, OAuth) post-MVP. |
| A5 | **MVP providers**: Clerk (users/count API), Supabase (PostgREST exact count on a table), Custom JSON endpoint (`{"totalUsers": n}` with a per-SaaS bearer token; verified only when host matches SaaS website host), Manual (unverified). | Fetch-only, no native deps, covers most indie SaaS auth stacks. Extensible `Provider` interface. | Add Firebase, Auth0, Postgres read-only. |
| A6 | Provider secrets are stored in the `integrations` table and **never returned by any query**; only server actions read them. Convex encrypts at rest. | Pragmatic for MVP. | Add envelope encryption with a KMS-style key in env. |
| A7 | **Sync cadence**: Convex cron every 4h, plus an immediate sync on connect and a manual "Sync now" (rate-limited to 1/10min). | Brief asks for "multiple updates per day, default 4h". | Tunable constant. |
| A8 | **New users** = delta of total users between snapshots (we don't get per-user signup events from every provider). | Uniform across providers. | Providers with event APIs can later report true signups. |
| A9 | Charts use **Recharts 3** with custom styling. | Mature, responsive, SSR-safe, easy to skin to blueprint look. | n/a |
| A10 | **Password reset** shipped as UI + Better Auth `requestPasswordReset` wired to console-logging email sender in dev; real email provider (Resend) is a post-MVP env toggle. | No email provider credentials available; don't block MVP. | Set `RESEND_API_KEY` and swap sender. |
| A11 | Logos/avatars are **URLs** (with a hosted-upload option via Convex storage). | Fast; Convex storage is available if needed. | n/a |
| A12 | Railway hosts the **Next.js server only**; Convex is hosted by Convex Cloud (team `thecodecave`, project `usertrack`). | Standard Convex deployment model. | n/a |
| A13 | Public pages are **dynamic** (no ISR) and read Convex via `fetchQuery`; Convex caches queries so this is cheap. OG images are cached by CDN headers for 1h. | Simplicity + freshness. | Add `revalidate` if traffic demands. |
| A14 | Leaderboard reads a **materialized `saasMetrics` row per SaaS**, recomputed after every snapshot, never recomputed on page load. | Performance requirement from the brief. | n/a |
| A15 | Seed/demo data: a small seeded set of demo SaaS entries flagged `isDemo` is used so the public leaderboard and landing page are never empty on day one. Demo entries are labeled and excluded from ranking once ≥5 real verified SaaS exist. | Shareability requires the product to look alive. | Remove seed via `internal.seed.clear`. |
| A-loop | **Model orchestration**: Fable 5.1 handles PLAN, BUILD, VERIFY/REVIEW and REPAIR (originally Opus 5 was to execute tickets). | Explicit user instruction mid-loop. | n/a |
| A16 | Chart palette: single series drawn in white (#f4f4f5, 2px) with pink (#fb0184) for area wash, end-dot and "new users" bars. The dataviz validator flags white as a categorical hue (chroma 0) — not applicable, there is one series and identity is carried by the title. CVD ΔE 34.7 and contrast ≥ 3:1 pass. | Brand direction mandates white linework + pink accent. | Add a second hue only if multi-series charts are introduced. |

## v0.2 decisions

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A17 | **Labelled demo rows stay in production** (they were already there) but are excluded from ranks, trending ranks, benchmarks, milestone feeds and flagged `demo: true` in the API. Removal is a one-liner and listed as a human decision. | Brief forbids seeding fake growth to look active; these predate v0.2, are visibly labelled, and prevent an empty board. | `npx convex run --prod seed:clear` |
| A18 | **One integration per role** (users / activation / traffic / revenue) instead of unlimited sources per SaaS. | Covers every roadmap capability with a simple mental model; avoids conflicting user-count sources. | Multi-source merge if demand appears. |
| A19 | **Canonical new-user counts are snapshot deltas** (net), uniform across providers; provider-reported gross signups only fill windows the snapshot history cannot cover yet. | Comparable across providers; new connections get real 30-day numbers immediately. | Expose gross vs net explicitly. |
| A20 | **Retention is estimated** as `active30d − new30d` over the cohort that existed 30 days ago, and shown only when a provider exposes active users. | No connected provider exposes true cohorts; honest labelling beats fabricated precision. | Cohorts from per-user activity APIs. |
| A21 | **First snapshot never creates threshold milestones**; it is a baseline. | A product connecting with 12,000 users has not "just crossed" 10k. | – |
| A22 | Trending **volume term is `log10(1+new)^1.5`** after testing showed a 60→100 product beating a 100k product adding 4k/week with a plain log. 50-user floor on relative growth; < 5 new users = no signal. | Brief: tiny products must not dominate; momentum still matters. | Tune exponent with real data. |
| A23 | **Under-review products are unranked but keep their public page**, with neutral wording ("Data under review") and auto-resolving flags. | Do not accuse; do not let anomalies drive rankings. | Manual review UI. |
| A24 | Firebase/GA4 auth uses **WebCrypto RS256 in the default Convex runtime** (probed: works) — no Node runtime needed. | Keeps all providers in one runtime; no cold starts. | – |
| A25 | **Boards sort the public set in one query** rather than a materialized board table; ranks and trending scores are precomputed. | Public set is small; one indexed read + in-memory sort is faster than N table writes per cycle. | Materialize past a few thousand products. |
| A26 | Public **API rate limit is an in-process token bucket** (60/min/IP). | No Redis in the stack; single Railway replica. | Edge rules if scaled. |
| A27 | Digest email is **Resend over HTTP** (no SDK) and silently in-app-only until credentials exist. | Zero dependencies; nothing breaks without creds. | – |
| A28 | Traffic and revenue are **private by default** and only public with an explicit per-SaaS switch. | Brief: only expose if the owner enables it. | – |
| A29 | Stripe MRR ignores discounts, trials and tax; `active` subscriptions only; up to 2,500 subscriptions per sync. | Simple, conservative, documented in the adapter. | Include `trialing`, discounts. |
| A30 | **Categories are a fixed list** of 15 slugs (`src/lib/categories.ts`) used for pages, filters and benchmarks; free tags remain. | Stable SEO URLs and benchmark groups. | Add on demand. |

## v0.3 — Public API keys & MCP

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A31 | **API keys are optional.** Anonymous access keeps the v0.2 behaviour (60 req/min per IP, shared 5-minute cache); a key only raises the quota to 1,000 req/day (+120/min burst) and turns off shared caching. | Backward compatible with every existing integration and badge; keys add accountability, not access. | Key-only endpoints if abuse appears. |
| A32 | **MCP is stateless Streamable HTTP** with JSON responses: one server + transport per request, no session ids, no server-initiated streams. | Works on a single Railway process behind a load balancer with no sticky sessions; every request re-validates the token. | Sessions/SSE if a client needs notifications. |
| A33 | **Onboarding MCP tokens expire after 7 days** and carry the default (all six) scopes; dashboard-created tokens have no expiry unless set (max 365 days). | Setup is a one-off; a short-lived token limits blast radius if it leaks from an agent config. | Auto-revoke once setup is marked done. |
| A34 | **`UT_GATEWAY_SECRET`** is one shared secret set identically on Railway (Next.js) and Convex prod; every gateway call carries it next to the token hash. Since SEC-1 the check **fails closed**: unset on Convex or mismatched → `unauthorized` (constant-time comparison, `convex/lib/gateway.ts`). Local dev needs it too (README setup). | Proves gateway calls come from the Next.js server, so a leaked token hash is useless against Convex directly. No per-request signing needed yet. | HMAC per request / Convex auth for service calls. |
| A35 | **No billing, one plan.** `PLANS.free` holds all limits; `planFor()` always returns `free`. | Nothing to charge for yet; the structure keeps call sites plan-agnostic. | Per-plan limits when paid tiers exist. |
| A36 | **Rate-limit numbers**: API key 1,000/day + 120/min; MCP 5,000 tool calls/day + 60/min; create ≤10 projects/hour/token; verify 20 s cooldown per project; sync 60 s per source; anonymous 60/min/IP. Daily quotas are bucketed counters in Convex, bursts are in-process buckets. | Reads generous, writes conservative: the write limits protect third-party provider APIs and prevent agent loops; the daily quota is far above any honest agent session. | Tune with usage data from `apiUsage`. |
| A37 | **Domain normalization strips `www.`**, lowercases, drops trailing dots and requires a dot in the host; same owner + same canonical domain = same project. Stored `websiteUrl` keeps its path. | Makes `usertrack_create_project` idempotent for agent retries and typos (`https://www.acme.com/` vs `acme.com`). Subdomains stay distinct (`app.acme.com` ≠ `acme.com`). | Public-suffix aware matching. |
| A38 | **Funnel events are stored in `auditLogs`** as `event:*` rows (`onboarding_ai_setup_selected`, `manual_setup_selected`, `mcp_setup_started`, `agent_prompt_copied`, `mcp_setup_completed`). | No third-party analytics in the stack; the audit table already has the right indexes and retention semantics. | Move to a dedicated analytics sink. |
| A39 | **API keys carry only `metrics:read`** and the REST routes do not check scopes (all data is public anyway); scopes matter for MCP tokens only. | Keeps key creation a one-click action; scope checks exist where writes exist. | Scoped keys if private API endpoints appear. |
| A40 | The **onboarding "Set up with AI" status is derived**, not stored: token `lastUsedAt`, audit entries, integration status, `lastSyncedAt`, `isPublic`. | No new state machine to keep in sync with what the agent actually did. | Explicit step events if derivation becomes ambiguous. |

## v0.4 decisions

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A41 | **PostgreSQL is aggregate-only and read-only by construction**: every session starts with `SET default_transaction_read_only = on`, the only statements are `count(*)` (with optional created-at / soft-delete / status filters), a `GROUP BY day` count, `version()`, and `pg_class` / `information_schema.columns` listings; identifiers must match `^[a-zA-Z_][a-zA-Z0-9_]*$` and are double-quoted; custom SQL (activation only) must be a single `SELECT`/`WITH` without `;`, write keywords or parameters other than `$1`. 10 s connect / 20 s statement timeouts. | The product's promise is "counts, never rows". A write attempt fails with `25006` even if the founder hands over a writable role; the wizard still tells them to create `usertrack_ro`. | Allow-list of safe functions in custom SQL if founders need more than one parameter. |
| A42 | **TCP database access lives in one Node action** (`convex/node/postgres.ts`, `pg` as `node.externalPackages`); adapters stay pure and declare `runtime() === "node"` + `toPostgres()`. Errors cross the boundary as `ConvexError { message, retryable }` and are re-thrown as `ProviderError`. | Keeps every fetch-based provider in the V8 runtime (no cold starts) and gives the sync engine one dispatch point instead of provider-specific branches. | A second Node action per database family (MySQL, Mongo) following the same shape. |
| A43 | **Supabase prefers the database mode** (session-pooler connection string, `auth.users` with `created_at` / `deleted_at`) and keeps the service-role API mode as a fallback; the mode is derived from the config (a connection string means database mode) and only `*.supabase.co` / `*.pooler.supabase.com` hosts are accepted. | Database mode is both more capable (verified per-window signups + 30-day history in one query) and less privileged (a `SELECT`-only role vs the full-access service key). | Drop API mode once nobody uses it. |
| A44 | **Firebase signup windows are computed by scanning `accounts:batchGet`** (pages of 1,000, keeping only `createdAt`) capped at `FIREBASE_SCAN_LIMIT = 100,000`; larger projects fall back to totals + snapshot deltas; `scanSignups: false` opts out. | Identity Toolkit has no count-by-date API; a bounded scan keeps per-user records out of memory beyond timestamps and keeps sync time predictable. | Firebase export to BigQuery / a customer-side count endpoint for very large projects. |
| A45 | **Source verification levels** are computed, not stored: `self_reported` when provider trust ≠ `verified` or kind is `manual`; `partially_verified` when a verified `users` source can read neither totals nor ranges; otherwise `verified`. Funnel-level verification is `verified` only when every stage is; any mix is `mixed`. | Today's adapters all read totals, so a source is in practice verified or self-reported; the middle level is reserved for derived-only adapters and makes the wording stable when they appear. | Store the level on the integration if computing it per read becomes expensive. |
| A46 | **Trending v2 constants**: 5 new-user floor, 50-user relative-growth floor, 10-user acceleration floor, acceleration clamped to `[−0.5, 2]`, trust default 60, freshness 1 → 0.5 between 24 h and 72 h then 0, history `0.6 + 0.4·min(1, days/14)`; under review → 0. `prev*` trending ranks are the position at the previous rerank. | Stale sources must not rank ("no evidence"), two weeks of history is the shortest window in which acceleration means anything, and a stable #1 should read "same", not "new" forever. | Tune with real data; the constants sit at the top of `convex/lib/trending.ts`. |
| A47 | **Benchmark cohorts need `MIN_SAMPLE = 5`**; smaller cohorts are deleted, only nine deciles and a count are stored, percentiles are rounded to steps of 5, and the public page states a position only in the top quarter. | With 2–3 members a "median" is a competitor's number; five is the smallest size where interpolated deciles stop being a lookup table, and the public set is still too small for 10. | Raise to 10 once category cohorts survive it. |
| A48 | **Discovery-feed identity comes from stored rows**: `milestone:{saasId}:{key}` and `{kind}:{saasId}:{day}`; `launched` / `verified` events are written once per SaaS (keyed by kind, stamped as `launchedAt` / `verifiedAt`). The feed is a merge of the two logs; nothing is synthesized at read time. | A feed that invents or repeats activity would contradict the "verified numbers" promise; stable ids make the API and UI idempotent. | Dedicated `feed` table if the merge of two `by_time` reads gets slow. |
| A49 | **Embed branding is mandatory**: every badge and the chart widget carry the UserTrack mark; `compact` drops the word, never the mark. Badges are public-metrics-only, cached 1 h at the edge, and rate-limited per IP (120/min) instead of keyed. | The mark is what makes a third-party number credible; keys would break README embeds. | White-label embeds as a paid feature. |
| A50 | **Compare "Indexed" mode rebases every product to 100 at its first day in the window** (chart: first daily total; API/MCP `index`: first positive daily total), `windowGrowthPct = last/base − 1`; series come from `dailyMetrics` only, at most four products, windows 7 / 30 / 90 / 365 / all. The OG image is a plain route (`/compare/og?s=&days=`) because `opengraph-image` files cannot read search params. | Products of different sizes are only comparable on relative growth; daily rows keep the query one indexed read per product. | Snapshot-resolution compare for 24h/7d if demanded. |
| A51 | **Funnel windows read `dailyMetrics` only** (≤ 2 × days indexed rows); when fewer than `min(days, 2)` rows exist, stages fall back to the materialized `saas` fields (90d falls back to the 30-day figures). Visitors / signups / activated are flows (sums), paying is a stock (last value). | Snapshots are irregular; daily rows make "previous period" well-defined and cheap. No separate funnel table is needed. | Hourly funnel once sub-day resolution matters. |
| A52 | **Time-to-activation is intentionally not implemented.** UserTrack only ever receives aggregate counts, and two cumulative series cannot be joined into per-user latencies; the activation section says so instead of estimating. | Estimating it would require per-user rows, which contradicts the aggregate-only contract of every provider and the endpoint template. | Only if a provider exposes an aggregate latency distribution. |

## v0.5 decisions — lifecycle, conversion, identity

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A53 | **UserTrack is not a revenue product.** Payment providers are conversion-status sources; `mrr` / `currency` / `payingUsers` are no longer written, are cleared by `migrations:lifecycleV1`, and no UI, API, share card, board or email shows amounts. Where a provider response contains amounts (RevenueCat overview, Lemon Squeezy) they are dropped at parse time. | Product direction: growth + conversion, not finance. | A separate, private-only revenue feature could reuse the conversion role, but must never be public. |
| A54 | **"Converted user" = unique subject with the configured monetization condition**; default mode `active_paid`, alternatives `ever_paid` / `first_payment` (identical for subscription APIs, differ only for endpoint data). A provider *customer* is never a converted user; trials never are. | Works for subscriptions, LTDs, one-time and usage pricing; keeps the UI business-model-agnostic. | Add `usage_threshold` if usage-based products ask for it. |
| A55 | **Conversion is private by default** (`visibility.conversionRate` / `trialConversion` / `convertedCount` = false); growth + activation stay public by default as before. Publishing a count implies publishing the rate. Legacy `showRevenue` maps to both conversion keys, `showTraffic` to `traffic`. | Connection ≠ publication; a founder can connect Stripe for private analytics. | The spec's suggested defaults had the rate checked; revisit if founders mostly switch it on. |
| A56 | **Funnel stages are flows**, including trial and converted (`newTrials`, `newConverted` per day); stocks are only a fallback (`kind: "stock"`). RevenueCat reports stocks only, so its daily flows are stock deltas clamped at 0 (churn never yields negative conversions); `newTrials` from providers that only report 7-day trial starts is spread as `newTrials7d / 7` per day. | One consistent window semantics ("converted in the last 30 days") across stages. | Per-day trial flows from RevenueCat need the customers API or webhooks. |
| A57 | **Trial → Converted** materialized rate = 30-day conversions ÷ 30-day trial starts (falls back to `converted / (trial + converted)` with stocks only); the Trial stage is shown only when the conversion source has the `trial` capability. | Matches the spec example (821 trials → 347 converted = 42.3 %). | Cohort-based trial conversion once identities exist. |
| A58 | **Identity subjects are salted SHA-256 hashes** (`IDENTITY_SALT` env, set on dev + prod) computed in the sync action; ids containing `@` are rejected; ≤ 5,000 ids per stage per sync; cohort engine caps 100k subjects per stage and runs from the daily sweep as an action. | No PII, no reversibility across projects, bounded work per mutation (Convex limits). | Larger products need incremental (since-last-sync) identity reporting. |
| A59 | **Cohort Verified** requires ≥ 20 signup subjects, ≥ 80 % signup coverage over the last 90 days and ≥ 70 % downstream match rate; otherwise `partially_mapped`; no links → `aggregate_only`. Benchmarks and boards use aggregate rates only. | Conservative thresholds so the badge means something. | Tune after real cohorts exist. |
| A60 | **RevenueCat owns only trial/converted**, via `metrics/overview` (`active_trials`, `active_subscriptions`), `active_paid` only, no identities in this phase (customer listing is too expensive; anonymous `$RCAnonymousID` ids would never be registered users anyway). Identity for mobile apps comes from Firebase Auth / Supabase / Auth0 / a database. | Cheap, exact, aggregate. | Customers API with `expand=items.active_entitlements` (bounded) or webhooks for cohorts. |
| A61 | **Stripe reads only `/v1/subscriptions`** per status (≤ 50 pages each): `paidAt = trial_end` after a trial else `start_date`; canceled-inside-trial and `incomplete*` never count; one-time payments are out of scope (endpoint provider). No `expand`, no invoices/charges. | Least privilege (Subscriptions → Read) and no amounts. | Checkout Sessions / PaymentIntents for one-time products, still amount-free. |
| A62 | **Postgres / Supabase identities** come from an optional `idColumn` (`auth.users.id` by default) with `LIMIT 5001 ORDER BY created_at DESC`; Clerk / Firebase / Auth0 report no identities (listing users is not aggregate). | Keep the aggregate-only contract; ids are hashed before leaving the action. | Clerk `GET /users` paging behind an explicit opt-in. |
| A63 | **Trending gets a small conversion multiplier** (≤ 1.10, `signupToConvertedPct` clamped at 25 %), activation stays 1.25 max; conversion is a supporting signal only. Conversion boards are secondary ("More rankings") and respect visibility. | Trending must stay about verified new users and velocity. | n/a |
| A64 | **Monthly reports may include private conversion numbers** (owner's own email); public surfaces never do. | The report goes to the founder only. | n/a |
| A70 | **Better Auth is a first-class `users` provider (`better_auth`) backed by the official plugin `@usertrack/better-auth`**, not the endpoint provider. Trust base 40, `trust: verified` regardless of hostname because the response is signed with a secret only the deployment holds. | Native verified source; no database credentials shared. | Domain verification could add a second signal. |
| A71 | **Protocol v1 is HMAC-SHA256 over `v1\n{REQUEST|RESPONSE}\n{path}\n{ts}\n{nonce}\n{sha256(body)}`**, headers `x-usertrack-{project,timestamp,nonce,signature}`, ±5 min, response bound to the request nonce; metrics is a `POST` so the body is signed (no query-string canonicalisation). | Simple, symmetric, WebCrypto-only, works in edge runtimes. | `protocolVersion` field allows v2. |
| A72 | **The protocol is implemented twice** (package + `convex/lib/betterAuthProtocol.ts`) and pinned by shared fixtures generated with `node:crypto`. | The package must not depend on the app; the Convex bundle must not depend on the package build (Railway stability). | Extract `@usertrack/protocol` once a second package needs it. |
| A73 | **Integration secrets `ut_int_…` live in `integrations.config`** (server-only, Convex at-rest encryption) plus SHA-256 + display prefix; shown once, rotatable, never returned by any query or MCP tool after creation. | Same storage model as every provider credential; UserTrack must sign with it, so a hash alone is not enough. | Envelope encryption (roadmap) covers it automatically. |
| A74 | **A Better Auth integration is created `awaitingVerification`** and excluded from the cron / "Sync now" until the first successful verify, so a not-yet-deployed plugin never produces failure runs, health emails or trust demotion. | The credential must exist before the founder can deploy. | — |
| A75 | **Lifecycle events are best effort** (fire-and-forget, 3 s timeout, deduped by `eventId`, pruned after 30 days) and only annotate the dashboard; snapshots from the pull remain the source of truth. Serverless hosts may drop events on freeze. | Auth latency and reliability must not depend on UserTrack. | `waitUntil` support where available. |
| A76 | **`user.deleted` events do not alter snapshots**; net growth is read from the plugin's counts on the next pull. `adapter.count` is required for exact counts; adapters without it use a bounded id-only scan (≤ 50k rows) and report `exactCounts: false`. | Better Auth hard-deletes users; totals already reflect it. | — |
| A65 | **projectType only steers onboarding, wording and store links** — no per-platform data model. `authMethods` are metadata; App Store / Google Play URLs are validated against the store domains. | The lifecycle model must stay uniform. | n/a |

## Email (v0.3)
- **Silence over noise.** Every non-transactional email is behind a preference, a dedupe key and a rule with an absolute floor. When in doubt the system does not send.
- **Welcome = verification** for email+password signups: Better Auth's `sendOnSignUp` verification mail is rendered as the welcome template with a "Confirm email & set up your profile" CTA (24h token). Google signups are verified already and get the plain welcome from the user-create trigger. Verification is not required to use the app (`requireEmailVerification: false`), so nothing about signup changed.
- **Source failure / recovery are transactional** (not disableable): a stale public page is a system-critical condition for a product whose whole point is verified numbers.
- **Weekly digest defaults to off, monthly report to on.** The mandatory report is the monthly one; the digest is an opt-in extra. Existing `profiles.digestOptIn` is ignored (kept in the schema for old rows).
- **Per-entity scheduled functions** (`scheduler.runAfter(24h, …)`) replace daily table scans for the two 24h reminders; they are durable across deploys and each carries a dedupe key. Sweeps that must scan (`noGrowthSweep`, `generateMonthly`, `digest.generate`) page through indexes in batches of 50–100 and reschedule themselves.
- **Ranking thresholds are board-size aware**: entering the Top 100 on a board of 8 products is not an achievement, so a threshold `t` only fires when more than `t` products are ranked.
- **Rank history** is one number per SaaS per closed day (`dailyMetrics.rank`, written by the daily sweep) — enough for "#14 → #8" in the monthly report without a new table.
- **Timezone** comes from the browser (`Intl.DateTimeFormat().resolvedOptions().timeZone`) when the profile is saved or the notification page is opened; unknown → UTC 09:00.
- **Recipient health** lives in `emailRecipients` keyed by address. Hard bounces and complaints suppress everything except password reset and verification, which are attempted regardless (a user locked out of their account is worse than one bounce).
- **Auth tokens are never persisted** by the email system: reset/verify URLs travel only as scheduled-function arguments (visible transiently in the Convex dashboard's scheduler view, expire in 1h/24h) and are not written into `emailEvents`.
- **30-day inactivity email is intentionally not implemented**: after the 7-day quiet email there is no second nudge until growth resumes and stops again — a second "still nothing" email adds no information.

## v0.7 decisions

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A77 | **One provider kind `native`** for every client of the native protocol; the adapter lives in `config.source` (`better-auth` · `prisma` · `drizzle` · `convex` · `authjs` · `custom`). `better_auth` is migrated by `migrations:nativeV1`, normalised on read and accepted as an input alias in the API / MCP / detection. | The v0.6 provider was already generic apart from names, base path and version gate; one kind keeps the ~10 registration points and every consumer (trust, funnel, API) unchanged. | Per-source trust or verification wording if adapters diverge. |
| A78 | **Protocol stays v1 with additive fields**: `users` / `activation` / `conversion` / `identities` blocks, `source`, `clientVersion` (`pluginVersion` / `provider` deprecated aliases), three new event types. The server parses both the nested and the 0.1.x users-only shape; new clients emit the nested shape only. | Wire-compatible: deployed 0.1.x plugins keep working without a bump; v2 reserved for incompatible changes. | Drop legacy parsing once no 0.1.x plugin reports in. |
| A79 | **The Convex protocol twin stays self-contained** (`convex/lib/nativeProtocol.ts`); a test cross-checks it against `packages/protocol/src` for the fixtures and random inputs. | The Convex bundler resolves `@usertrack/protocol` via `node_modules` → `dist/`, which only exists after a package build; making `convex dev` / the Railway build depend on it is exactly the fragility A72 avoided. | Point the package `exports` at source for the workspace if Convex adds workspace-aware bundling. |
| A80 | **Multi-role native sources are sibling integration rows** sharing one credential: a users pull reports `reported.roles`, and `recordReported` inserts `native` rows for activation / conversion that have no source yet (never replacing an explicit one); rotation re-keys the siblings. | The whole engine assumes one integration per role; auto-attaching keeps "install → done" without touching sync, funnel or API. | A first-class multi-role integration if per-role config diverges. |
| A81 | **Prisma adapter** uses `model.count({ where: { [createdAtField]: { gte, lt }, ...where } })` and a `$extends` query extension (`query.<model>.create / delete` — `async ({ args, query }) => { const r = await query(args); … }`) for the push hook; `createMany` returns no rows and is not hooked (verified against the Prisma client-extensions docs). Duck-typed — no `@prisma/client` dependency. | Aggregate-only, works for any model with a timestamp; `createdAtField: null` for models without one. | Hook `createManyAndReturn` when common. |
| A82 | **Drizzle adapter** runs `db.select({ n: count() }).from(table).where(and(where, gte(col, from), lt(col, to)))` (`count()` from `drizzle-orm` maps to a number); `drizzle-orm` is an optional peer dependency imported only by the `/drizzle` sub-export; tests render the condition with `PgDialect` instead of a live database. | Works with every driver; `db.$count` exists only in newer releases. | Use `db.$count` once `>=0.34` is the floor. |
| A83 | **Convex adapter** wraps the handler in the founder's `httpAction` (`convexHandler(opts)` → `(ctx, req)`), counts through an `internalQuery` using the built-in `by_creation_time` index and `countWithCap` (`take(cap + 1)`, default cap 10,000 → `exact: false` beyond it, shown as approximate); base URL = the `.convex.site` URL. | Convex has no cheap `count`; `@convex-dev/aggregate` is the documented way to exact counts at scale. | Ship an aggregate-component variant of the query. |
| A84 | **Auth.js adapters expose no count**: counting goes through the Prisma / Drizzle user model (`source: "authjs"` uses `prismaUsers(prisma.user, { createdAtField: null })` in the default snippet because the stock Auth.js `User` model has no `createdAt`); push uses `events.createUser({ user })`. | Matches the Auth.js adapter interface and default schemas. | Detect a `createdAt` column from the Prisma schema in the setup plan. |
| A85 | **Recommendation priority**: native ranks first for strong signals (Better Auth, Auth.js / NextAuth, Convex — they own the user store); ORM-only signals (Prisma, Drizzle, Lucia, Kysely, Mongoose, custom) rank after hosted auth providers (no code change) but before a raw Postgres connection; `prisma:postgresql` / `drizzle-pg` now mean native. The endpoint provider is only recommended when nothing is detected (or for Amplitude / Mixpanel activation). | A Prisma app on Clerk should keep Clerk; a Prisma app without a hosted auth provider is better served by the verified SDK than by sharing a database role. | Tune with real onboarding data. |
| A86 | **Default base paths**: `/api/auth` for Better Auth (metrics at `<base>/usertrack/metrics`), `/api/usertrack` for every other source (metrics at `<base>/metrics`); the signed `path` is the protocol constant, independent of where the handler is mounted. | Better Auth owns its base path; a single route file `app/api/usertrack/metrics/route.ts` is the natural mount for the SDK. | — |
| A87 | **Events route moved** to `/api/integrations/native/events`; `/api/integrations/better-auth/events` re-exports the handler and passes the legacy path so 0.1.x signatures (which cover the path) still verify. | Plugins in the wild must keep working across the rename. | Remove the legacy route with the next protocol version. |
| A88 | **Screenshots are committed** under `docs/screenshots/v0.7` at 1× device scale (the v0.6 script wrote to `/tmp`). | The task asks for reviewable screenshots; 1× keeps the repository small. | Move to an external bucket if the folder grows. |


## Founder identity & sharing (v0.8)
| # | Assumption | Why | Revisit |
|---|---|---|---|
| A90 | **Founder aggregates run over public projects only** and after per-metric visibility; activation is weighted (Σ activated / Σ users of projects with an activation source), the previous-window change is undefined unless every project has one, and averages of percentages are never computed. | A profile must not leak a private project or a private count, and a mean of rates is meaningless across sizes. | Materialize on the profile row when founders have many projects. |
| A91 | **Aggregate history forward-fills each project** from its last known daily total and counts 0 before its first day. | A sync gap must not read as a user drop across the founder's portfolio. | n/a |
| A92 | **Share cards keep the real min→max chart scale and print MAX / MIN and the window's first / last date**; the y-axis does not start at zero. | Zero-based axes flatten every mature product into a line; printing the scale keeps the min-based chart honest. | Offer a zero-based toggle if founders ask. |
| A93 | **"Verified by UserTrack" appears only when `saas.trust === "verified"`**; everything else says "Tracked on UserTrack"; founder cards say verified only when all public projects are verified. | The verification claim must match the provenance model exactly. | n/a |
| A94 | **Card configuration lives in the URL** (defaults omitted) and is never persisted; only share events, preferences and posts are stored. | Deterministic, cacheable PNGs; no table of preview variations. | Saved templates if requested. |
| A95 | **Share events reuse milestone / event keys** and add per-day spike keys and per-month benchmark keys, so cooldowns fall out of the keys; floors: users ≥ 100, activated ≥ 1K, converted ≥ 100, records ≥ 100, streak ≥ 30 days, spike ≥ 3×, benchmark ≥ p90. | One coherent event model instead of a parallel milestone table; nothing minor becomes a card. | Tune floors with real data. |
| A96 | **No calendar-month growth card**: monthly growth is represented by the existing +25/50/100 % in-30-days milestones, because a rolling-30d chart labelled as a calendar month would mislead. | Anti-misleading rule beats feature count. | Add real calendar-month rows if wanted. |
| A97 | **Auto-posting is opt-in per category, defaults off, ≤ 1 post per founder per day, one attempt per event, no retries.** The bot account posts verified-only major events, ≤ 3 per day, and respects `allowPromotion` / `allowTagging`. | UserTrack must never become a spam bot; when uncertain, do not post. | n/a |
| A98 | **OAuth state is bound to the signed-in profile, single-use, 10 minutes; PKCE S256; token exchange and posting run in Convex actions with `X_CLIENT_*` on Convex, not Railway.** Tokens are never returned or logged; disconnect revokes inside the action so no token travels through scheduler args. | Secrets in one place, no CSRF, no token leakage. | Envelope encryption (roadmap). |
| A99 | **The UserTrack account uses OAuth 1.0a user-context credentials** (never expire) instead of OAuth 2.0 tokens that would rotate on refresh and need persisting. | Nothing to store or refresh for the platform account. | n/a |
| A100 | **Posts carry the share URL and rely on the OG unfurl for the image** (no media upload). | Keeps the free-tier API surface small; the OG image is the card. | v2 media upload. |
| A101 | **Founder and product X handles are separate concepts**; only the founder handle exists today (`profiles.x`); drafts accept a `projectHandle` for later. | Never conflate the two. | Add `saas.x`. |
| A102 | **Card routes are rate-limited per IP (40/min) and cached 5 minutes**; the Share Studio fetches through the browser cache. | Image rendering is the most expensive public endpoint. | CDN in front (`HUMAN_TODO`). |

## Discovery, history, benchmarks v2, datasets, webhooks (v0.9)
| # | Assumption | Why | Revisit |
|---|---|---|---|
| A103 | **Ranking history is one row per (project, board, window, UTC day)** holding the day's last position, patched within the day and never afterwards; the 6 intra-day reranks are not stored individually. | Daily granularity answers every product question (movers, charts, best rank) at 1/6 of the rows; intra-day changes are 4-hour-cycle noise. | Store every rerank if intra-day analysis is ever wanted. |
| A104 | **Biggest Movers compare with the stored position 7 days ago** (closest stored day within 7–10 days), never with the previous refresh; products without a stored position a week ago have no movement. | "#78 → #31 this week" must come from history, not from the last 4-hour cycle. | Add 30-day movers once history spans months. |
| A105 | **Benchmark history is one row per product per ISO week** with the product's own standings (value, percentile, cohort median, size); cohort deciles themselves are not versioned. | Everything a founder needs ("up from Top 27 % last month") in one small row per week; versioning deciles would multiply rows per cohort without a consumer. | Version aggregates if cohort-level trend analysis is requested. |
| A106 | **Age cohorts are two families**: `age:<bucket>` from the founder-entered founding month, `tracked:<bucket>` from the first stored snapshot; a product is in exactly one of them and labels say which. | The brief forbids confusing product age with tracking age; mixing them in one cohort would make the label a lie. | Import founding dates from Crunchbase / Product Hunt. |
| A107 | **Size buckets stay at five** (`0-100`, `100-1k`, `1k-10k`, `10k-100k`, `100k+`) instead of the finer eight suggested in the brief. | The public set is small — finer buckets only produce cohorts below the floor — and the keys are part of filter URLs and share cards. | Split buckets when any exceeds ~200 members. |
| A108 | **`MIN_SAMPLE` is 10** for every cohort × metric (conversion included). | Ten is the smallest cohort where nine interpolated deciles stop being a lookup table of individual values; the plan since v0.4. | Raise further with the public set. |
| A109 | **Public benchmark statements need the owner's "Benchmark statement" visibility (default on)** and stay top-quarter only; benchmark history is public only under the same gate and without values. | Selective exposure per the brief, without changing what founders already had. | Per-metric opt-in if founders ask. |
| A110 | **Webhook events are the project owner's**: only endpoints of the owner receive a project's events; rank-change events fire at most once per UTC day per project; demo products never emit. | Machine subscriptions must not leak other founders' activity; 4-hour rank flapping would spam consumers. | Public "firehose" webhooks for followed products if demanded (separate consent model). |
| A111 | **Webhook URLs must be public HTTPS**; private ranges, metadata hosts, `.internal` / `.local` names and the platform's own hosts are refused at creation and the hostname is re-resolved through DNS-over-HTTPS right before every delivery. `fetch` cannot pin the resolved address, so a resolver flipping within the same second is a residual risk. | SSRF is the brief's stated critical risk; Convex actions have no raw sockets, so DoH is the available resolver. | Deliver through an egress proxy that pins IPs. |
| A112 | **Webhook secrets are stored in plaintext in Convex** (server-side only) and only ever read by the delivery action; they are returned exactly once on create / rotate. | HMAC needs the plaintext; a KMS envelope is the same roadmap item as integration configs. | Envelope encryption. |
| A113 | **Retry schedule 0 / 5 min / 30 min / 2 h / 12 h (5 attempts)**, retries only for 5xx, 408, 425, 429, timeouts and network errors; other 4xx are final; 25 consecutive failures disable the endpoint. | Standard industry policy; a misconfigured endpoint should not consume retries for a day. | Configurable per endpoint. |
| A114 | **Datasets are computed from the same public projection as the pages (≤ 100 rows per window)** and paginated with an opaque offset cursor. | The public set is small; one code path keeps pages and datasets consistent. | Materialized board table (`docs/ROADMAP.md`). |
| A115 | **Monthly ranking pages exist only for boards with ≥ 3 rankable products**, frozen on the 1st; nothing is generated retroactively for months without stored data. | The brief forbids thin pages and fake history. | Backfill from `rankHistory` once it spans full months. |
| A116 | **Discovery events are stored, keyed and never synthesized at read time**: `rank_jump` (≥ 10 places into the top 50, 7-day cooldown), `traction` (once, ≤ 30 days listed and ≥ 100 new users/week), `benchmark` (monthly, top 10 % of a category cohort). | One event model serves the public feed, the watchlist, webhooks and share cards without drift. | Tune thresholds with real data. |
| A117 | **Follow notification sub-preferences default to on under an off master switch**; rows written before v0.9 read the missing keys as on. | Conservative by default (nothing is mailed until the founder opts in), no surprise loss of mails for existing opt-ins. | — |
| A118 | **History charts break the line across gaps ≥ 3 days (daily), ≥ 14 (weekly), ≥ 45 (monthly)** and downsample 1Y to weekly and ALL to weekly / monthly points; raw rows are kept. | Honest gaps and bounded payloads without destroying history. | LTTB if week/month buckets ever hide meaningful shape. |
| A119 | **Gateway secret fails closed everywhere** (`gateway.authenticate`, `embeds.record`, `native.ingestEvent`) through one helper; `embeds.record` / `ingestEvent` keep their `{ ok: false }` / `401` return shapes instead of throwing so the Next.js routes and SDK clients see the same responses as before. | One place to audit; a misconfigured deployment can no longer silently expose the gateway. | — |
| A120 | **No application-level encryption for `integrations.config`** in v1.0; every "encrypted at rest" phrase was replaced with "stored server-side, never returned to the dashboard, the API or an agent", which is what the code actually guarantees. | Honest wording beats an unimplemented promise; envelope encryption is a separate backlog item. | Envelope encryption with a KMS-style key (`docs/BACKLOG.md`). |
| A121 | **Every `next` / `redirectTo` parameter goes through `safeInternalPath`** (`src/lib/safe-redirect.ts`, also imported by `convex/social.ts`): only a single leading `/`, no `\\`, no control characters, no scheme / `@` before the first `/`, rejected again after percent-decoding and after resolving against a dummy origin; otherwise the fallback. Query strings survive. | Closes open redirects in the auth form and the X OAuth round trip with one pure, tested function. | — |
| A122 | **Security headers live in `next.config.ts`**: HSTS (2 y, preload), nosniff, `Referrer-Policy strict-origin-when-cross-origin`, `Permissions-Policy`, CSP with `frame-ancestors 'none'` + `X-Frame-Options DENY` on every route except the embeddable ones (`/embed/*`, `/api/badge/*`, `/api/embed/*`, `/widget.js`), which get `frame-ancestors *` and no `X-Frame-Options` via a negative-lookahead matcher. `script-src` keeps `'unsafe-inline'` (Next.js inline bootstrap, no nonce plumbing yet) and already allows the Rybbit host; `upgrade-insecure-requests` is emitted only when `NEXT_PUBLIC_SITE_URL` is https so local builds on `http://localhost` keep working. | Pragmatic set that does not break the app, verified with curl and a browser sign-in. | Nonce-based CSP without `'unsafe-inline'`. |
| A123 | **Email+password accounts must verify their address before signing in** (`requireEmailVerification: true`); sign-up no longer auto-signs in and shows a "Check your inbox" state; the sign-in form turns Better Auth's `EMAIL_NOT_VERIFIED` into an inline notice with a resend button. Google accounts are verified at signup and unaffected. Existing sessions are untouched, which is why publishing is gated separately (A124). | Prevents squatting on someone else's address and keeps public founder identity honest. | — |
| A124 | **Publishing needs a verified email**: `saas.setPublic` / `updateProject(isPublic: true)` / MCP `usertrack_update_project { isPublic: true }`, a public founder profile (`profiles.upsert` unless `profilePublic: false`) and `completeOnboarding` throw `forbidden: "Verify your email to publish (Settings → Verify email)"`. Unpublishing, edits and private profiles are never gated. | Sessions that predate verification (or were created before SEC-1) must not publish under an unverified address. | — |
| A126 | **Per-minute rate limits are durable in Convex via the official `@convex-dev/rate-limiter` component**, with the in-memory bucket kept as a free first line (`limit()` = local `take` → `rateLimits.check`). Chosen limits: `anonApi` 60/min, `apiKey` 120/min, `mcp` 120/min (`PLANS.free.mcp.burstPerMinute` raised from 60 as the ticket specified; `/developers` and the OpenAPI text derive from `PLANS`), `badge` 120/min, `embed` 60/min shared by the iframe and its JSON refresh (was 120 — a widget refreshes every 5 minutes, so 60 per IP is ample), `card` 40/min, `nativeEvents` 600/min per project, `xCallback` fixed window 10 per 10 min. No shards: the busiest single key (`nativeEvents`, 10 QPS) is within Convex's OCC retry budget. | One catalog for both sides; the component is transactional, fails closed under contention and needs nothing beyond `convex deploy`. | Add `shards` to `nativeEvents` if OCC conflicts show up in the Convex logs; per-plan limits when plans exist. |
| A127 | **`limit()` denies on `OptimisticConcurrencyControlFailure` and degrades to the local bucket on any other Convex error** (network, misconfigured gateway secret), logging `[rate-limit]`. The smoke test showed that 25 parallel requests on one IP exhaust Convex's automatic OCC retries on that key's single `rateLimits` document; treating that as "allowed" would let the flood through, treating it as 429 (`Retry-After: 1`) is exactly right because only the flooding key is affected. `rateLimits.check` itself fails closed on a wrong or missing gateway secret (`requireGateway`). | A Convex outage already breaks the data behind these routes; the limiter must not turn a partial outage into a full one, but contention on a single key is the abuse case, not an outage. | `shards` on the per-IP limits would spread the contention (at the cost of earlier false denials per shard); fail closed for `xCallback` / `nativeEvents` if abuse during outages becomes a concern. |
| A130 | **Providers block only *our own* hostnames (`SITE_URL`, `NEXT_PUBLIC_SITE_URL`, `CONVEX_SITE_URL`, `CONVEX_CLOUD_URL`) plus `*.convex.cloud`, not the whole `*.convex.site` / `*.railway.app` suffixes the ticket listed.** Webhooks keep refusing `*.convex.site` as before. | Convex-hosted founder apps legitimately expose their `@usertrack/node` handler on `https://<deployment>.convex.site` (documented in `nativeSetup.ts`) and Railway default domains are ordinary hosting targets; blocking the suffixes would break real integrations without adding protection beyond "not our deployment". | Add suffixes to `BLOCKED_HOST_SUFFIXES` in `convex/lib/ssrf.ts` if a loop/amplification case appears. |
| A131 | **The fetch-time DNS guard lives in `convex/providerRun.ts` (`assertPublicHosts(p.hosts(config))`), not inside each provider's `fetch`.** | Every sync, verify (`gateway.verifyIntegration`, `integrations.test` / `verifyStored`) and backfill already goes through `providerRun`; centralizing it keeps providers pure, keeps the 30+ existing provider unit tests (which stub `fetch`) untouched, and guarantees one generic error for every refusal. A DNS answer is still checked immediately before the request; `fetch` cannot pin the resolved address, so the residual rebinding window is the same as for webhooks (A-row of v0.9). | Move into providers if a provider ever fetches hosts that are not derivable from its config. |
| A132 | **Native base URLs no longer accept `http://localhost` / `127.0.0.1`.** | The founder's app must be reachable from Convex cloud anyway; the dev exception was an SSRF hole. Local end-to-end testing of `@usertrack/node` uses the package test-suite (`packages/*`), which never goes through the provider. | Re-add behind `UT_ALLOW_PRIVATE_DB`-style flag if a self-hosted dev loop needs it. |
| A133 | **Blocked hosts return no `durationMs` at all** (the existing failure shapes of `integrations.test`, `verifyStored` and `usertrack_verify_integration` never carried timing) and the guard runs before any TCP / HTTP request, so no port-scan oracle exists; the ticket's "durationMs: 0" is therefore satisfied by omission. | Adding a zero field to the failure DTOs would change the public MCP / API contract for no benefit. | — |
| A128 | **Better Auth sign-in is not rate-limited by SEC-2.** Better Auth executes inside Convex's HTTP router (proxied through `/api/auth/[...all]`), so neither `limit()` nor a trusted `x-forwarded-for` reaches it without new plumbing; a `signIn` (20 per 10 min per IP) limit is a backlog item, not a silent catalog entry. Better Auth's own built-in limiter is not enabled either (it would need a storage table in the auth component). | The ticket allowed skipping when Better Auth is not easily hookable; adding an unenforced name to the catalog would be misleading. | Backlog: `hooks.before` in `convex/auth.ts` + trusted IP header from the proxy. |
| A129 | **`X-RateLimit-Remaining` for anonymous / keyed-burst responses is read back from the component (`getValue` after `limit`) inside the same mutation**, at the cost of one extra component query per request; the MCP route, badge, embed and card routes never expose remaining counts. | The ticket asked for the headers to reflect the durable state; `limit()` alone only returns `ok` / `retryAfter`. | Drop the read if Convex function time on `/api/v1/*` becomes noticeable. |
| A125 | **The publish-blocked message is parsed with `/Uncaught \w*Error: ([^\n]*)/`** at the two dashboard publish sites (manage page switch, onboarding "Publish page"); the pre-existing app-wide `/^.*Uncaught Error: /` pattern never matches Convex's multi-line format and shows "Server Error" instead. Only the SEC-1 sites were changed. | Surgical: the ticket needs a clear message here; the app-wide fix is a backlog item next to error tracking. | Shared `errMsg` helper + `ConvexError` for user-facing failures. |
