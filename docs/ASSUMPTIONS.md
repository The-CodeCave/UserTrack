# Assumptions Log

Decisions made autonomously during the CodeCraft loop. Each entry: what was assumed, why, and how to revisit.

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A1 | **Next.js 16 App Router** (not Vite/CRA). | Metadata API, `next/og` ImageResponse, RSC + Convex `fetchQuery` for SEO pages. | n/a |
| A2 | **Auth data lives inside Convex** via `@convex-dev/better-auth` component. | Single backend, no external Postgres, Better Auth remains the auth engine. Docs pin `better-auth ~1.6.x`; `better-auth` is **pinned to exactly 1.6.15**: 1.6.30 breaks `ConvexBetterAuthProvider`'s `AuthClient` typing (`useSession().data: never`). pnpm uses `node-linker=hoisted` (`.npmrc`) so only one copy of `better-auth` types exists. | If the component blocks, fall back to Better Auth + Railway Postgres and mirror user ids into Convex. |
| A3 | **Ranking formula** = verified net new users in trailing 30 days (`total_now − total_30d_ago`), tiebreak by 30d growth %. | Simple, explainable, growth-first, not size-first. | Could add a blended score later. |
| A4 | **Trust levels**: `verified` (auto-synced from a 3rd-party auth provider or a domain-matched endpoint), `unverified` (manual/self-reported or non-domain-matched endpoint), `pending` (connected, no successful sync yet). | Matches brief; keeps manual entry possible but never ranked as verified. | Add stronger verification (DNS TXT, OAuth) post-MVP. |
| A5 | **MVP providers**: Clerk (users/count API), Supabase (PostgREST exact count on a table), Custom JSON endpoint (`{"totalUsers": n}` with a per-SaaS bearer token; verified only when host matches SaaS website host), Manual (unverified). | Fetch-only, no native deps, covers most indie SaaS auth stacks. Extensible `Provider` interface. | Add Firebase, Auth0, Postgres read-only. |
| A6 | Provider secrets are stored in the `integrations` table and **never returned by any query**; only server actions read them. Convex encrypts at rest. | Pragmatic for MVP. | Add envelope encryption with a KMS-style key in env. |
| A7 | **Sync cadence**: Convex cron every 4h, plus an immediate sync on connect and a manual "Sync now" (rate-limited to 1/10min). | Brief asks for "multiple updates per day, default 4h". | Tunable constant. |
| A8 | **New users** = delta of total users between snapshots (we don't get per-user signup events from every provider). | Uniform across providers. | Providers with event APIs can later report true signups. |
| A9 | Charts use **Recharts 3** with custom styling. | Mature, responsive, SSR-safe, easy to skin to blueprint look. | n/a |
| A10 | **Password reset** shipped as UI + Better Auth `requestPasswordReset` wired to console-logging email sender in dev; real email provider (Resend) is a post-MVP env toggle. | No email provider credentials available; don't block MVP. | Set `RESEND_API_KEY` and swap sender. |
| A11 | Logos/avatars are **URLs** (with a hosted-upload option via Convex storage). | Fast; Convex storage is available if needed. | n/a |
| A12 | Railway hosts the **Next.js server only**; Convex is hosted by Convex Cloud (team `thecodecave`, project `usertrack`). | Standard Convex deployment model. | n/a |
| A13 | Public pages are **dynamic** (no ISR) and read Convex via `fetchQuery`; Convex caches queries so this is cheap. OG images are cached by CDN headers for 1h. | Simplicity + freshness. | Add `revalidate` if traffic demands. |
| A14 | Leaderboard reads a **materialized `saasMetrics` row per SaaS**, recomputed after every snapshot, never recomputed on page load. | Performance requirement from the brief. | n/a |
| A15 | Seed/demo data: a small seeded set of demo SaaS entries flagged `isDemo` is used so the public leaderboard and landing page are never empty on day one. Demo entries are labeled and excluded from ranking once ≥5 real verified SaaS exist. | Shareability requires the product to look alive. | Remove seed via `internal.seed.clear`. |
| A-loop | **Model orchestration**: Fable 5.1 handles PLAN, BUILD, VERIFY/REVIEW and REPAIR (originally Opus 5 was to execute tickets). | Explicit user instruction mid-loop. | n/a |
| A16 | Chart palette: single series drawn in white (#f4f4f5, 2px) with pink (#fb0184) for area wash, end-dot and "new users" bars. The dataviz validator flags white as a categorical hue (chroma 0) — not applicable, there is one series and identity is carried by the title. CVD ΔE 34.7 and contrast ≥ 3:1 pass. | Brand direction mandates white linework + pink accent. | Add a second hue only if multi-series charts are introduced. |

## v0.2 decisions

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A17 | **Labelled demo rows stay in production** (they were already there) but are excluded from ranks, trending ranks, benchmarks, milestone feeds and flagged `demo: true` in the API. Removal is a one-liner and listed as a human decision. | Brief forbids seeding fake growth to look active; these predate v0.2, are visibly labelled, and prevent an empty board. | `npx convex run --prod seed:clear` |
| A18 | **One integration per role** (users / activation / traffic / revenue) instead of unlimited sources per SaaS. | Covers every roadmap capability with a simple mental model; avoids conflicting user-count sources. | Multi-source merge if demand appears. |
| A19 | **Canonical new-user counts are snapshot deltas** (net), uniform across providers; provider-reported gross signups only fill windows the snapshot history cannot cover yet. | Comparable across providers; new connections get real 30-day numbers immediately. | Expose gross vs net explicitly. |
| A20 | **Retention is estimated** as `active30d − new30d` over the cohort that existed 30 days ago, and shown only when a provider exposes active users. | No connected provider exposes true cohorts; honest labelling beats fabricated precision. | Cohorts from per-user activity APIs. |
| A21 | **First snapshot never creates threshold milestones**; it is a baseline. | A product connecting with 12,000 users has not "just crossed" 10k. | – |
| A22 | Trending **volume term is `log10(1+new)^1.5`** after testing showed a 60→100 product beating a 100k product adding 4k/week with a plain log. 50-user floor on relative growth; < 5 new users = no signal. | Brief: tiny products must not dominate; momentum still matters. | Tune exponent with real data. |
| A23 | **Under-review products are unranked but keep their public page**, with neutral wording ("Data under review") and auto-resolving flags. | Do not accuse; do not let anomalies drive rankings. | Manual review UI. |
| A24 | Firebase/GA4 auth uses **WebCrypto RS256 in the default Convex runtime** (probed: works) — no Node runtime needed. | Keeps all providers in one runtime; no cold starts. | – |
| A25 | **Boards sort the public set in one query** rather than a materialized board table; ranks and trending scores are precomputed. | Public set is small; one indexed read + in-memory sort is faster than N table writes per cycle. | Materialize past a few thousand products. |
| A26 | Public **API rate limit is an in-process token bucket** (60/min/IP). | No Redis in the stack; single Railway replica. | Edge rules if scaled. |
| A27 | Digest email is **Resend over HTTP** (no SDK) and silently in-app-only until credentials exist. | Zero dependencies; nothing breaks without creds. | – |
| A28 | Traffic and revenue are **private by default** and only public with an explicit per-SaaS switch. | Brief: only expose if the owner enables it. | – |
| A29 | Stripe MRR ignores discounts, trials and tax; `active` subscriptions only; up to 2,500 subscriptions per sync. | Simple, conservative, documented in the adapter. | Include `trialing`, discounts. |
| A30 | **Categories are a fixed list** of 15 slugs (`src/lib/categories.ts`) used for pages, filters and benchmarks; free tags remain. | Stable SEO URLs and benchmark groups. | Add on demand. |

## v0.3 — Public API keys & MCP

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A31 | **API keys are optional.** Anonymous access keeps the v0.2 behaviour (60 req/min per IP, shared 5-minute cache); a key only raises the quota to 1,000 req/day (+120/min burst) and turns off shared caching. | Backward compatible with every existing integration and badge; keys add accountability, not access. | Key-only endpoints if abuse appears. |
| A32 | **MCP is stateless Streamable HTTP** with JSON responses: one server + transport per request, no session ids, no server-initiated streams. | Works on a single Railway process behind a load balancer with no sticky sessions; every request re-validates the token. | Sessions/SSE if a client needs notifications. |
| A33 | **Onboarding MCP tokens expire after 7 days** and carry the default (all six) scopes; dashboard-created tokens have no expiry unless set (max 365 days). | Setup is a one-off; a short-lived token limits blast radius if it leaks from an agent config. | Auto-revoke once setup is marked done. |
| A34 | **`UT_GATEWAY_SECRET`** is one shared secret set identically on Railway (Next.js) and Convex prod; every gateway call carries it next to the token hash. If unset on Convex the check is skipped (dev convenience). | Proves gateway calls come from the Next.js server, so a leaked token hash is useless against Convex directly. No per-request signing needed yet. | HMAC per request / Convex auth for service calls. |
| A35 | **No billing, one plan.** `PLANS.free` holds all limits; `planFor()` always returns `free`. | Nothing to charge for yet; the structure keeps call sites plan-agnostic. | Per-plan limits when paid tiers exist. |
| A36 | **Rate-limit numbers**: API key 1,000/day + 120/min; MCP 5,000 tool calls/day + 60/min; create ≤10 projects/hour/token; verify 20 s cooldown per project; sync 60 s per source; anonymous 60/min/IP. Daily quotas are bucketed counters in Convex, bursts are in-process buckets. | Reads generous, writes conservative: the write limits protect third-party provider APIs and prevent agent loops; the daily quota is far above any honest agent session. | Tune with usage data from `apiUsage`. |
| A37 | **Domain normalization strips `www.`**, lowercases, drops trailing dots and requires a dot in the host; same owner + same canonical domain = same project. Stored `websiteUrl` keeps its path. | Makes `usertrack_create_project` idempotent for agent retries and typos (`https://www.acme.com/` vs `acme.com`). Subdomains stay distinct (`app.acme.com` ≠ `acme.com`). | Public-suffix aware matching. |
| A38 | **Funnel events are stored in `auditLogs`** as `event:*` rows (`onboarding_ai_setup_selected`, `manual_setup_selected`, `mcp_setup_started`, `agent_prompt_copied`, `mcp_setup_completed`). | No third-party analytics in the stack; the audit table already has the right indexes and retention semantics. | Move to a dedicated analytics sink. |
| A39 | **API keys carry only `metrics:read`** and the REST routes do not check scopes (all data is public anyway); scopes matter for MCP tokens only. | Keeps key creation a one-click action; scope checks exist where writes exist. | Scoped keys if private API endpoints appear. |
| A40 | The **onboarding "Set up with AI" status is derived**, not stored: token `lastUsedAt`, audit entries, integration status, `lastSyncedAt`, `isPublic`. | No new state machine to keep in sync with what the agent actually did. | Explicit step events if derivation becomes ambiguous. |

## v0.4 decisions

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A41 | **PostgreSQL is aggregate-only and read-only by construction**: every session starts with `SET default_transaction_read_only = on`, the only statements are `count(*)` (with optional created-at / soft-delete / status filters), a `GROUP BY day` count, `version()`, and `pg_class` / `information_schema.columns` listings; identifiers must match `^[a-zA-Z_][a-zA-Z0-9_]*$` and are double-quoted; custom SQL (activation only) must be a single `SELECT`/`WITH` without `;`, write keywords or parameters other than `$1`. 10 s connect / 20 s statement timeouts. | The product's promise is "counts, never rows". A write attempt fails with `25006` even if the founder hands over a writable role; the wizard still tells them to create `usertrack_ro`. | Allow-list of safe functions in custom SQL if founders need more than one parameter. |
| A42 | **TCP database access lives in one Node action** (`convex/node/postgres.ts`, `pg` as `node.externalPackages`); adapters stay pure and declare `runtime() === "node"` + `toPostgres()`. Errors cross the boundary as `ConvexError { message, retryable }` and are re-thrown as `ProviderError`. | Keeps every fetch-based provider in the V8 runtime (no cold starts) and gives the sync engine one dispatch point instead of provider-specific branches. | A second Node action per database family (MySQL, Mongo) following the same shape. |
| A43 | **Supabase prefers the database mode** (session-pooler connection string, `auth.users` with `created_at` / `deleted_at`) and keeps the service-role API mode as a fallback; the mode is derived from the config (a connection string means database mode) and only `*.supabase.co` / `*.pooler.supabase.com` hosts are accepted. | Database mode is both more capable (verified per-window signups + 30-day history in one query) and less privileged (a `SELECT`-only role vs the full-access service key). | Drop API mode once nobody uses it. |
| A44 | **Firebase signup windows are computed by scanning `accounts:batchGet`** (pages of 1,000, keeping only `createdAt`) capped at `FIREBASE_SCAN_LIMIT = 100,000`; larger projects fall back to totals + snapshot deltas; `scanSignups: false` opts out. | Identity Toolkit has no count-by-date API; a bounded scan keeps per-user records out of memory beyond timestamps and keeps sync time predictable. | Firebase export to BigQuery / a customer-side count endpoint for very large projects. |
| A45 | **Source verification levels** are computed, not stored: `self_reported` when provider trust ≠ `verified` or kind is `manual`; `partially_verified` when a verified `users` source can read neither totals nor ranges; otherwise `verified`. Funnel-level verification is `verified` only when every stage is; any mix is `mixed`. | Today's adapters all read totals, so a source is in practice verified or self-reported; the middle level is reserved for derived-only adapters and makes the wording stable when they appear. | Store the level on the integration if computing it per read becomes expensive. |
| A46 | **Trending v2 constants**: 5 new-user floor, 50-user relative-growth floor, 10-user acceleration floor, acceleration clamped to `[−0.5, 2]`, trust default 60, freshness 1 → 0.5 between 24 h and 72 h then 0, history `0.6 + 0.4·min(1, days/14)`; under review → 0. `prev*` trending ranks are the position at the previous rerank. | Stale sources must not rank ("no evidence"), two weeks of history is the shortest window in which acceleration means anything, and a stable #1 should read "same", not "new" forever. | Tune with real data; the constants sit at the top of `convex/lib/trending.ts`. |
| A47 | **Benchmark cohorts need `MIN_SAMPLE = 5`**; smaller cohorts are deleted, only nine deciles and a count are stored, percentiles are rounded to steps of 5, and the public page states a position only in the top quarter. | With 2–3 members a "median" is a competitor's number; five is the smallest size where interpolated deciles stop being a lookup table, and the public set is still too small for 10. | Raise to 10 once category cohorts survive it. |
| A48 | **Discovery-feed identity comes from stored rows**: `milestone:{saasId}:{key}` and `{kind}:{saasId}:{day}`; `launched` / `verified` events are written once per SaaS (keyed by kind, stamped as `launchedAt` / `verifiedAt`). The feed is a merge of the two logs; nothing is synthesized at read time. | A feed that invents or repeats activity would contradict the "verified numbers" promise; stable ids make the API and UI idempotent. | Dedicated `feed` table if the merge of two `by_time` reads gets slow. |
| A49 | **Embed branding is mandatory**: every badge and the chart widget carry the UserTrack mark; `compact` drops the word, never the mark. Badges are public-metrics-only, cached 1 h at the edge, and rate-limited per IP (120/min) instead of keyed. | The mark is what makes a third-party number credible; keys would break README embeds. | White-label embeds as a paid feature. |
| A50 | **Compare "Indexed" mode rebases every product to 100 at its first day in the window** (chart: first daily total; API/MCP `index`: first positive daily total), `windowGrowthPct = last/base − 1`; series come from `dailyMetrics` only, at most four products, windows 7 / 30 / 90 / 365 / all. The OG image is a plain route (`/compare/og?s=&days=`) because `opengraph-image` files cannot read search params. | Products of different sizes are only comparable on relative growth; daily rows keep the query one indexed read per product. | Snapshot-resolution compare for 24h/7d if demanded. |
| A51 | **Funnel windows read `dailyMetrics` only** (≤ 2 × days indexed rows); when fewer than `min(days, 2)` rows exist, stages fall back to the materialized `saas` fields (90d falls back to the 30-day figures). Visitors / signups / activated are flows (sums), paying is a stock (last value). | Snapshots are irregular; daily rows make "previous period" well-defined and cheap. No separate funnel table is needed. | Hourly funnel once sub-day resolution matters. |
| A52 | **Time-to-activation is intentionally not implemented.** UserTrack only ever receives aggregate counts, and two cumulative series cannot be joined into per-user latencies; the activation section says so instead of estimating. | Estimating it would require per-user rows, which contradicts the aggregate-only contract of every provider and the endpoint template. | Only if a provider exposes an aggregate latency distribution. |

## v0.5 decisions — lifecycle, conversion, identity

| # | Assumption | Rationale | Revisit |
|---|-----------|-----------|---------|
| A53 | **UserTrack is not a revenue product.** Payment providers are conversion-status sources; `mrr` / `currency` / `payingUsers` are no longer written, are cleared by `migrations:lifecycleV1`, and no UI, API, share card, board or email shows amounts. Where a provider response contains amounts (RevenueCat overview, Lemon Squeezy) they are dropped at parse time. | Product direction: growth + conversion, not finance. | A separate, private-only revenue feature could reuse the conversion role, but must never be public. |
| A54 | **"Converted user" = unique subject with the configured monetization condition**; default mode `active_paid`, alternatives `ever_paid` / `first_payment` (identical for subscription APIs, differ only for endpoint data). A provider *customer* is never a converted user; trials never are. | Works for subscriptions, LTDs, one-time and usage pricing; keeps the UI business-model-agnostic. | Add `usage_threshold` if usage-based products ask for it. |
| A55 | **Conversion is private by default** (`visibility.conversionRate` / `trialConversion` / `convertedCount` = false); growth + activation stay public by default as before. Publishing a count implies publishing the rate. Legacy `showRevenue` maps to both conversion keys, `showTraffic` to `traffic`. | Connection ≠ publication; a founder can connect Stripe for private analytics. | The spec's suggested defaults had the rate checked; revisit if founders mostly switch it on. |
| A56 | **Funnel stages are flows**, including trial and converted (`newTrials`, `newConverted` per day); stocks are only a fallback (`kind: "stock"`). RevenueCat reports stocks only, so its daily flows are stock deltas clamped at 0 (churn never yields negative conversions); `newTrials` from providers that only report 7-day trial starts is spread as `newTrials7d / 7` per day. | One consistent window semantics ("converted in the last 30 days") across stages. | Per-day trial flows from RevenueCat need the customers API or webhooks. |
| A57 | **Trial → Converted** materialized rate = 30-day conversions ÷ 30-day trial starts (falls back to `converted / (trial + converted)` with stocks only); the Trial stage is shown only when the conversion source has the `trial` capability. | Matches the spec example (821 trials → 347 converted = 42.3 %). | Cohort-based trial conversion once identities exist. |
| A58 | **Identity subjects are salted SHA-256 hashes** (`IDENTITY_SALT` env, set on dev + prod) computed in the sync action; ids containing `@` are rejected; ≤ 5,000 ids per stage per sync; cohort engine caps 100k subjects per stage and runs from the daily sweep as an action. | No PII, no reversibility across projects, bounded work per mutation (Convex limits). | Larger products need incremental (since-last-sync) identity reporting. |
| A59 | **Cohort Verified** requires ≥ 20 signup subjects, ≥ 80 % signup coverage over the last 90 days and ≥ 70 % downstream match rate; otherwise `partially_mapped`; no links → `aggregate_only`. Benchmarks and boards use aggregate rates only. | Conservative thresholds so the badge means something. | Tune after real cohorts exist. |
| A60 | **RevenueCat owns only trial/converted**, via `metrics/overview` (`active_trials`, `active_subscriptions`), `active_paid` only, no identities in this phase (customer listing is too expensive; anonymous `$RCAnonymousID` ids would never be registered users anyway). Identity for mobile apps comes from Firebase Auth / Supabase / Auth0 / a database. | Cheap, exact, aggregate. | Customers API with `expand=items.active_entitlements` (bounded) or webhooks for cohorts. |
| A61 | **Stripe reads only `/v1/subscriptions`** per status (≤ 50 pages each): `paidAt = trial_end` after a trial else `start_date`; canceled-inside-trial and `incomplete*` never count; one-time payments are out of scope (endpoint provider). No `expand`, no invoices/charges. | Least privilege (Subscriptions → Read) and no amounts. | Checkout Sessions / PaymentIntents for one-time products, still amount-free. |
| A62 | **Postgres / Supabase identities** come from an optional `idColumn` (`auth.users.id` by default) with `LIMIT 5001 ORDER BY created_at DESC`; Clerk / Firebase / Auth0 report no identities (listing users is not aggregate). | Keep the aggregate-only contract; ids are hashed before leaving the action. | Clerk `GET /users` paging behind an explicit opt-in. |
| A63 | **Trending gets a small conversion multiplier** (≤ 1.10, `signupToConvertedPct` clamped at 25 %), activation stays 1.25 max; conversion is a supporting signal only. Conversion boards are secondary ("More rankings") and respect visibility. | Trending must stay about verified new users and velocity. | n/a |
| A64 | **Monthly reports may include private conversion numbers** (owner's own email); public surfaces never do. | The report goes to the founder only. | n/a |
| A65 | **projectType only steers onboarding, wording and store links** — no per-platform data model. `authMethods` are metadata; App Store / Google Play URLs are validated against the store domains. | The lifecycle model must stay uniform. | n/a |

## Email (v0.3)
- **Silence over noise.** Every non-transactional email is behind a preference, a dedupe key and a rule with an absolute floor. When in doubt the system does not send.
- **Welcome = verification** for email+password signups: Better Auth's `sendOnSignUp` verification mail is rendered as the welcome template with a "Confirm email & set up your profile" CTA (24h token). Google signups are verified already and get the plain welcome from the user-create trigger. Verification is not required to use the app (`requireEmailVerification: false`), so nothing about signup changed.
- **Source failure / recovery are transactional** (not disableable): a stale public page is a system-critical condition for a product whose whole point is verified numbers.
- **Weekly digest defaults to off, monthly report to on.** The mandatory report is the monthly one; the digest is an opt-in extra. Existing `profiles.digestOptIn` is ignored (kept in the schema for old rows).
- **Per-entity scheduled functions** (`scheduler.runAfter(24h, …)`) replace daily table scans for the two 24h reminders; they are durable across deploys and each carries a dedupe key. Sweeps that must scan (`noGrowthSweep`, `generateMonthly`, `digest.generate`) page through indexes in batches of 50–100 and reschedule themselves.
- **Ranking thresholds are board-size aware**: entering the Top 100 on a board of 8 products is not an achievement, so a threshold `t` only fires when more than `t` products are ranked.
- **Rank history** is one number per SaaS per closed day (`dailyMetrics.rank`, written by the daily sweep) — enough for "#14 → #8" in the monthly report without a new table.
- **Timezone** comes from the browser (`Intl.DateTimeFormat().resolvedOptions().timeZone`) when the profile is saved or the notification page is opened; unknown → UTC 09:00.
- **Recipient health** lives in `emailRecipients` keyed by address. Hard bounces and complaints suppress everything except password reset and verification, which are attempted regardless (a user locked out of their account is worse than one bounce).
- **Auth tokens are never persisted** by the email system: reset/verify URLs travel only as scheduled-function arguments (visible transiently in the Convex dashboard's scheduler view, expire in 1h/24h) and are not written into `emailEvents`.
- **30-day inactivity email is intentionally not implemented**: after the 7-day quiet email there is no second nudge until growth resumes and stops again — a second "still nothing" email adds no information.

