# Changelog

## v1.0.1 review fixes (unreleased)
- **FIX-5 — share pages 500 in production; the sign-in bounce was a shared rate-limit bucket.** *(a)* `src/lib/og/share-card.tsx` and `src/lib/og/board.tsx` read Convex through `fetchQuery` (`cache: "no-store"`) inside ISR routes (`/s/[slug]/share/[kind]` with `revalidate = 300`, every board / founder `opengraph-image`), which Next rejects at render time (`DYNAMIC_SERVER_USAGE`); they now use `publicQuery` like every other public page. *(b)* The `src/proxy.ts` route guard called `<deployment>.convex.site/api/auth/convex/token` without the `x-ut-client-ip` header, so Better Auth keyed **every visitor's** token fetch (one per `/app` navigation and per link prefetch) on the single `no-trusted-ip|/convex/token` bucket of 100/min — past that, everyone was redirected to `/sign-in?next=`. The guard now trusts a still-valid `convex_jwt` cookie without a round trip, stamps the trusted client IP when it does ask, and passes the request through (instead of bouncing) when Convex answers with an error or a 429; the app shell reads `profiles.me` only once `useConvexAuth()` reports the session, so the client no longer sees a spurious `null` while the token is being set. `/convex/jwks` (the public key set Convex itself fetches to validate JWTs, also without a client IP) is exempt from the limiter. Tests: `src/proxy.test.ts`.
- **HIST-1 — the first sync backfills the whole user history, not 30 days.** *(a) Reach per provider.* `Provider.historyLimit(config)` (`convex/providers/types.ts`, `historyLimit()` in `providers/index.ts`) replaces the global `BACKFILL_DAYS` for the first sync: `full` = 1,826 days for PostgreSQL, Supabase database mode and Firebase (one `GROUP BY day` / one scan), `bounded` = 365 days for Clerk, Supabase API mode and Auth0 (one request per day), 90 for the native SDK (protocol v1 `MAX_HISTORY_DAYS`); traffic sources keep 30 + 7 rolling. `fillDaily` starts a full-reach series at the first signup when nothing is older than the window (the Postgres/Supabase query and the Firebase scan already read everything since the cap; leading empty days are not written). *(b) Chunked writes.* `sync.runBackfill` → `writeHistory` converts signup series to totals once (`reconstructTotals`, `convex/lib/history.ts` — which also fixes the old off-by-one: today's signups were never subtracted, so every reconstructed day was a day late and the live day's `newUsers` was yesterday's) and writes 365-day chunks through `sync.recordHistory` (now returns `{ written, prev }`, takes `prev` for exact deltas across boundaries), then `sync.finishHistory` fixes the live day's baseline and runs `recomputeDerived` once; `finishBackfill` stores the summed `pointsWritten`. `getForSync` also returns `totalUsers`. *(c) Nothing retroactive.* `dailyMetrics.backfilled` (schema, optional) marks reconstructed rows; `daily.run` excludes them from `dailyMilestones`; the import path has no milestone / spike / mail / share / webhook hooks (test: an 800-day series with a 500-signup day leaves `milestones`, `events`, `emailEvents`, `shareEvents`, `webhookDeliveries` empty, also after the sweep). *(d) Dashboard + API.* "Backfill entire history" next to "last 30 days" (`integrations.backfill { days: number | "all" }`, clamped to the provider reach in `sync.backfill`; `integrationView.historyDays` tells the UI "up to 5 years / 365 days"); `public.history` and `GET /api/v1/saas/{slug}/history` return `reconstructedUntil`, and the growth chart labels those points in the footer and tooltip ("reconstructed from signup dates"). Wizard hints and the no-source nudge mail no longer promise "30-day history". Tests: `convex/history.test.ts` (chunking with exact totals across boundaries, idempotent re-run, Clerk capped at 365 / Postgres at 1,826, no retroactive events, `reconstructedUntil`), `convex/lib/history.test.ts` (`reconstructTotals`), `convex/providers/providers.test.ts` (`historyLimit` matrix, `fillDaily`), `firebase.test.ts`. Docs: `HISTORY.md`, `PROVIDERS.md`, `ASSUMPTIONS.md` (A228–A230).
- **LOOPS-1 — three loops closed: the growth streak is visible, the watchlist reaches the dashboard with an unseen state, and every outbound link is attributable.** *(a) Streak.* `saas.bestStreakDays` (schema, optional) is patched by the daily sweep whenever `streakDays(rows)` beats it (`convex/daily.ts`); new `StreakChip` (`src/components/blueprint/streak-chip.tsx`, amber `--color-new`, Flame) on dashboard product cards from 3 days, in the manage header (with `best N` when the best is higher, plus an *at risk* state and "No signups in the last 24h — streak at risk" when `newUsers24h === 0`) and on the public page from 7 days when verified and growth is public. `stripPrivate` drops `streakDays` / `bestStreakDays` with the growth switch; `streakDays` joins the public metrics DTO (`/api/v1/saas/{slug}/metrics`) and the MCP project summary. *(b) Watchlist.* `profiles.feedSeenAt` + `follows.markFeedSeen` (once per visit of `/app/following`); `follows.feed` returns `seenAt` + `unseenCount`, counted inside `watchlistFeed` over the whole window before the `limit` slice (REST `/following` and MCP `usertrack_get_watchlist` unchanged). The dashboard's static Following panel became "From your watchlist · 7d" (`follows.feed { days: 7, limit: 5 }`, amber "N new" pill, See all →, nothing-followed and quiet-week states); the row renderer moved to `src/components/app/watchlist-feed-item.tsx` and is shared with `/app/following`; the sidebar shows an amber count and the mobile tab a dot while `unseenCount > 0` (`rank_change` items are dated by the last sync, so a persisting move counts as new again after a sync). *(c) Attribution.* `attributedUrl(url, { ref, source, medium, campaign })` + `shareLinkUrl(slug, kind, channel)` in `src/lib/site.ts` (pure, tested; `widgetPageUrl` now built on it). Outbound share links are attributed per channel — `ShareButtons` (`x` / `link`), Share Card Studio, Share Center, `ai-setup`, X auto-posts (`x-founder` / `x-bot`, `convex/social.ts`), MCP share objects (`mcp`) — while canonical / OG / API URLs stay clean. Badge snippets (public block, configurator, `usertrack_get_embed_code`) link back with `ref=badge&utm_source=badge&utm_medium=image&utm_campaign=<type>`, and `/api/badge/[slug]` records the referring host like the widget route (`src/lib/embed-host.ts` shared, `embeds.record { kind: "widget" | "badge" }`, `embedSites.badgeLoads` next to `loads`, still one row per host, widget / badge labels on the manage page and configurator) — best effort and documented as such: the SVG is edge-cached for 1h and GitHub's camo proxy sends no Referer. Emails route every app link through `link(c, path, emailType)` → `ref=email&utm_source=email&utm_medium=<type>&utm_campaign=<kind|path>`; signed prefs / unsubscribe / verify / reset links are untouched. Sign-up first touch: `<Attribution/>` in the root layout stores sanitized (`[a-z0-9_-]`, ≤ 40 chars) `ref` / `utm_*` once in `localStorage["ut:attribution"]` (30-day expiry on read), `ProfileForm` sends it, `profiles.upsert` re-validates and writes `profiles.attribution` on insert only (never public); `sign_up_completed` carries `ref?`. Tests **677 / 87 files** (+11): `bestStreakDays` in `jobs.test.ts`, growth gating in `visibility.test.ts`, `unseenCount` before / after `markFeedSeen`, badge loads in `embeds.test.ts`, an email UTM assertion, `site.test.ts`, `attribution.test.ts`, `embed-host.test.ts`. Docs: `FOLLOWS.md`, `ROADMAP.md` (item 7 unread state ticked), `ANALYTICS.md` (catalog + *Attribution* section + Rybbit note), `API.md` (metrics `streakDays`, badge section), `PROFILES.md`, `/privacy` 3.2. Screenshots: `docs/screenshots/v1/loops/` via `scripts/shots-loops.mjs`.
- **SHIP-2 — the interim waitlist joins `main`, and the whole tree is re-verified from clean.** `apps/waitlist/` (previously the `waitlist` branch, 54 files, nothing outside that folder) is merged with `--no-ff`. It stays a **separate deployable**: its own `pnpm-workspace.yaml` (`packages: []`), lockfile, `node_modules`, Convex project (`usertrack-waitlist`, prod `glad-lynx-143`) and Railway service. The merge surfaced one real break — the repo root type-checks and lints `**/*.ts(x)`, so the waitlist's own Vite / Tailwind toolchain produced 3 `tsc` errors and 13 ESLint errors in a tree that was green before. Fixed the way `packages` already is: `apps` added to `exclude` in `tsconfig.json` and to `globalIgnores` in `eslint.config.mjs` (`vitest.config.ts` was already scoped to `src/**` + `convex/**`, and `.github/workflows/ci.yml` already has `paths-ignore: ["apps/**", …]`). Full clean verification on the merged tree: `rm -rf .next && pnpm install --frozen-lockfile`, lint **0 errors / 8 warnings**, typecheck, **666 tests / 84 files**, `packages:build` + `packages:typecheck` + `packages:test` (**61**: 9 protocol · 22 node · 30 better-auth), `pnpm build`, then `PORT=3100 pnpm start` with `scripts/smoke.mjs`, the FIX-4 embeddable-header checks, `/leaderboard` (`Cache-Control: public, s-maxage=300, stale-while-revalidate=1800` + HSTS / CSP `frame-ancestors 'none'` / `X-Frame-Options: DENY`), `/api/badge/demo-northwind.svg` (`frame-ancestors *`, no `X-Frame-Options`), `/api/health` and `?deep=1` (`convex: "ok"` + a `jobs` array carrying the FIX-2 `running` / `stale` fields) and a 25-request loop against `/api/auth/sign-in/email` — requests 1–20 reach Better Auth, 21+ answer `429` with `x-retry-after` from the durable FIX-4 limiter. Transcript: `docs/screenshots/v1/ship2/api-checks.txt`; screenshots (landing, `/leaderboard`, `/s/demo-northwind`, `/app/settings` at 375 / 768 / 1440) in `docs/screenshots/v1/ship2/` via the new `scripts/shots-ship2.mjs`. Docs: `docs/RELEASE-v1.0.md` gains a *v1.0.1 review fixes* section (per-ticket changes, the two new env vars, the post-deploy `leaderboard:rerank` step, the residual-risk list) and its waitlist section is rewritten from "must not be merged" to how the separate deployable works; `README.md` (`apps/waitlist` paragraph + layout, 666 tests, 52 MCP tools), `docs/DEPLOYMENT.md` (topology + a *The interim waitlist* deploy section), `docs/BACKLOG.md`, `docs/ASSUMPTIONS.md` (A227), `docs/CODECRAFT.md` (the review-driven loop), `HUMAN_TODO.md` (launch checklist re-numbered to 15 required steps).
- **FIX-4 — the Better Auth endpoints have a rate limit that survives a redeploy and a second replica; embeddable headers, IPv6 SSRF normalisation and `trustmrr.status` closed out.** *(a) Durable auth rate limit.* `/api/auth/*` is served by Better Auth inside the Convex HTTP router, so its default limiter counted requests in whichever Convex isolate answered — a limit in name only. `createAuth` now passes `rateLimit.customStorage`, whose `consume(key, rule)` is one `internal.authRateLimits.consume` mutation over the new `authRateLimits` table (`by_key`, `by_lastRequest`): check and increment happen in one Convex transaction, which is exactly the atomic step the 1.6.22 storage contract asks for (`get` / `set` are implemented too and never used). Rules (`convex/lib/authRateLimits.ts`): `/sign-in/*` 20 per 10 min, `/sign-up/*` 10 per hour, `/send-verification-email` and `/forget-password` / `/request-password-reset` 5 per 10 min, everything else 100/min. The bucket key is `<ip>|<path>`, and the IP comes from the FIX-1 trust rules: the Next.js `/api/auth/[...all]` route now resolves `clientIp(req)` itself and stamps `x-ut-client-ip` on the proxied request, which `advanced.ipAddress.ipAddressHeaders` is pointed at — a raw `x-forwarded-for` arriving at Convex has one hop too many to be trustworthy (A223). Expired buckets are swept 20 at a time on the transaction that opens a new one. `registerRoutes` builds a static auth with `{}` as ctx, so the limiter is only attached when the context can actually run a mutation (A224). *(b) Embeddable headers* are held together by the ordering of three `headers()` entries in `next.config.ts`; `src/lib/security-headers.test.ts` now evaluates those entries with Next's own `getPathMatch` and asserts `frame-ancestors *` + no `X-Frame-Options` on `/embed/*`, `/api/badge/*`, `/api/embed/*`, `/widget.js`, and `frame-ancestors 'none'` + `X-Frame-Options: DENY` on `/`, `/leaderboard`, `/app`, `/s/*`, `/api/v1/*`. *(c) `convex/lib/ssrf.ts`* recognised only the compressed `::ffff:` prefix, so `0:0:0:0:0:ffff:7f00:1` passed as public; `expandIpv6` now normalises brackets, zone ids, `::`, leading zeros and a dotted-quad tail into eight groups before the range check (v4-mapped, v4-compatible, `::`, `::1`, ULA, link-local, multicast, NAT64). *(d) `trustmrr.status`* was an unauthenticated query that told any visitor whether `TRUSTMRR_API_KEY` is set; it now requires a founder profile and answers `{ configured: false }` otherwise — the only caller is the signed-in project form. *(e) Docs*: `docs/ARCHITECTURE.md` → *Public caching* now names which public routes are ISR (static shell) and which render per request with cached data + a CDN `Cache-Control` (A225), and the Cloudflare cache rule moved from *Recommended* to **required step 14** in `HUMAN_TODO.md` with the exact expression. No `revalidate` export was removed — Next 16 does not warn about one on a request-time-dynamic route. Tests: `convex/authRateLimits.test.ts` (pure decision, the storage mutations, and a real `betterAuth()` handler over the durable store that 429s the 6th verification mail and the 21st `/sign-in/social`), `convex/lib/ssrf.test.ts`, `convex/trustmrr.test.ts`, `src/lib/security-headers.test.ts` — 666 tests / 84 files.
- **FIX-3 — the rerank is a scheduler chain, and the homepage is a bounded read again.** *(a) `leaderboard.rerank`* was one `internalAction` that looped every `runQuery` page and every `runMutation` page inline: at a few thousand products it walks into Convex's 10-minute action limit and dies with half the table re-ranked and the `jobRuns` row never closed. It is now three chained `internalMutation`s over the new `rankScratch` table — phase 1 (`rerank`) pages `saas` (50) and writes one compact scratch row per product, phase 2 (`rankPlan`) reads them once, computes the leaderboard order and the 24h/7d/30d trending orders with the same pure comparators, stamps the ranks back onto the scratch rows and writes `publicStats`, phase 3 (`applyRanks`) pages the scratch rows, applies the patches / rank history / milestones / rank-jump events / webhooks exactly as before and deletes each row it applied. Every step carries the same `runId` (FIX-2 lock) and the same `now`; only phase 3 records pages, so a run still reports `pages = ceil(products / 50)` and `items = products`, and a per-project failure is still caught, counted and skipped. A chain that dies between pages leaves scratch rows behind, so the next run drops up to 500 of them at the entry point. Ceiling moved from "10 minutes of action time" to "phase 2 is one transaction" ≈ 8k products (A219); the accumulator was measured at ~200 bytes per product, so scheduler arguments (~10 MB at 50k) were not an option. `internal.leaderboard.rerank` is a **mutation** now — crons, `ctx.scheduler.runAfter` call sites and `npx convex run leaderboard:rerank` are unchanged, tests drive the chain with `finishAllScheduledFunctions`. *(b) `public.landing`* (from the landing-v2 branch) had re-introduced the unbounded read OPS-2 removed: `publicSet` `take(5000)` plus, for each of 110 rows, an owner read and an unbounded `dailyMetrics.collect()`. It is now `boardRows(most-users, limit 100, verifiedOnly false)` + `boardRows(new-rising, 7d, limit 10)` + the `publicStats` row, both with the new `hideDemo` board filter so demo products stay out exactly as before. The Top 100 renders dense (position, logo, name, total users) and reads neither owner nor sparkline, so those rows no longer carry them (A221); `sparkline` itself is `take(30)` off `by_saas_day` descending instead of a `collect`. Measured under a 600-document transaction limit on a 1,000-product seed: **~430 documents** instead of ~5,000 + 110 unbounded history reads. *(c)* `boardRows` now treats a missing index key as the lowest score (`?? -Infinity`) — which is where Convex's index order puts it — so a row whose `growth24hPct` was never materialized ends the walk instead of dragging it to `MAX_BOARD_SCAN`; `HUMAN_TODO.md` step 9 gained the one manual `leaderboard:rerank` right after `convex deploy` that materializes the key for products listed before v1.0. Tests: `convex/jobs.test.ts` (250 projects through the full chain vs. the single-shot reference, empty `rankScratch`, counters written, plus a dead chain's leftovers swept), `convex/boardScan.test.ts` (landing parity on the 40-product seed with ties + the 1,000-product read-cost budget) and the unchanged assertions of `boards`/`history`/`discovery`/`gateway`. 656 tests / 82 files, screenshots `docs/screenshots/v1/FIX-3` (375 / 768 / 1440, identical to before). Docs: ARCHITECTURE → Background jobs + Public caching, A219–A222.
- **FIX-2 — a running lock on `jobRuns`, so no cron can start a job whose previous chain is still paging.** `startRun` (`convex/jobs.ts`) is now an *acquire*: the entry point of every paged driver (cursor **and** `runId` undefined) reads the latest `jobRuns` row of its job (`by_job_time`, desc) and returns `null` when that row is still open and younger than the job's TTL — `null` means insert nothing, read nothing, re-schedule nothing, and `console.warn` `job <name> still running, skipped`. Before this a `daily sweep` / `weekly digest` / `monthly growth report` firing while the previous chain was mid-table re-walked the pages the first chain had not reached (a second batch of digest / report emails is only stopped by the dedupe key, not by design), and a second `leaderboard.rerank` applied `prevRank` / `prevTrendingRank` a second time on top of ranks the first pass had already written. Covered: `daily.run`, `daily.benchmarks` (+ its `benchmark standings` sub-run), `daily.snapshotRankings`, `trust.dailyReview`, `share.benchmarkSweep`, `digest.generate` (the owner-triggered preview never takes the lock), `email.reports.generateMonthly`, `email.lifecycle.noGrowthSweep`, `retention.sweep`, `cohorts.rebuildAll`, `sync.runAll`, `leaderboard.rerank` and `social.refreshFollowers` — which now records a `follower refresh` run like every other driver. A run left open past its TTL is closed as `lastError: "abandoned (lock expired)"` and the new trigger proceeds; TTLs live in `LOCK_TTL_MS` (rerank 30 min · sync 3 h · digest & monthly report 12 h · everything else the 6 h default). The TTL is the backstop, not the path: every driver body is wrapped so an unexpected throw closes its run through `failRun` (mutations, where a rethrow would roll the release back) or `failActionRun` + rethrow (actions, where nothing rolls back) with `errors + 1` and the message on `lastError`. `/api/health?deep=1` now reports `running` and `stale` (open past its TTL) per job. Tests: `convex/jobs.test.ts` (skip while open, release on completion, abandon on expiry, per-job TTLs, a `daily sweep` cron double-fire that stays at one run of one page, a skipped rerank that leaves every `rank` unset, a throwing page that closes its run and frees the lock) and `convex/emailFunctions.test.ts` (60 opted-in profiles, so the first page of 50 leaves the run open: the second `digest.generate` trigger adds no `jobRuns` row, no second page and no extra `weekly-digest` event).
- **FIX-1 — five correctness fixes from the security/ops review.** (a) **Trusted client IP behind Cloudflare** (`src/lib/client-ip.ts`): `clientIp()` now reads `cf-connecting-ip`, then `true-client-ip`, before the last `x-forwarded-for` hop — but only when `UT_TRUST_CF_HEADERS=1` (new env, default off, both headers are forgeable when the origin is reachable directly). Without it a proxied `usertrack.dev` would collapse every visitor into one rate-limit bucket. (b) **MCP profile publish gate** (`convex/gateway.ts` `updateProfileTool`): switching a hidden profile back to `profilePublic: true` now calls `requireVerifiedToPublish` on the Better Auth user (`authComponent.getAnyUserById`), like `profiles.upsert` and `updateProjectTool`; unverified tokens get `forbidden`, hiding a profile still needs nothing. (c) **Analytics only in production** (`src/lib/analytics.ts`): the production Rybbit site id is no longer a hardcoded default — new `resolveSiteId()` implies it only for `NODE_ENV=production` **and** `NEXT_PUBLIC_SITE_URL` starting with `https://usertrack.dev`, so `pnpm dev`, CI builds and Railway preview services no longer send pageviews and `identify()` into the production site. An explicit `NEXT_PUBLIC_RYBBIT_SITE_ID` always wins (empty = kill switch); the Convex helper already required `RYBBIT_SITE_ID` and the CSP host allowlist is unchanged. (d) **Health-check accuracy** (`src/app/api/health/route.ts`): `?deep=1` runs the `public.stats` probe and the gateway-gated `jobs.health` probe **separately** — `convex: ok|down` now reflects Convex alone, and a missing `UT_GATEWAY_SECRET` answers `jobs: "unavailable: gateway secret not configured"` instead of making a healthy deployment look down; the 3 s race timer is `clearTimeout`-ed once it settles. (e) **`jobRuns.finishedAt`** (`convex/jobs.ts` `recordPage`): the patch only carries `finishedAt` when `done === true` — an explicit `undefined` deletes the field in Convex, so a later non-final page silently un-finished an already-closed run. Docs: `ARCHITECTURE.md` (trust model), `DEPLOYMENT.md` (env table + health check), `ANALYTICS.md`, `API.md`, `MCP.md`, `.env.example`, `HUMAN_TODO.md` (step 12 phase C + the Rybbit step). Tests: `client-ip.test.ts` (gate on/off, IPv6, missing headers), `analytics.test.ts` (`resolveSiteId`), `route.test.ts` (both ok · stats ok + gateway missing · timeout), `convex/publishGate.test.ts`, `convex/jobs.test.ts`.
- **FIX-0** — `landing-v2` merged into `main` (fast-forward, 4 commits: pink scale + trust/positive/negative/new semantic tokens, `convex/public.ts` `landing` query, list-first homepage with New & hot carousel and Top 100 table). Baseline on the merged tree is green with no fixes needed: lint 0 errors (8 pre-existing warnings), typecheck, **636 tests / 82 files**, build. `.codecraft-loop-*.md` loop notes gitignored.

## v1.0 launch hardening (unreleased)
- **SHIP-1 — consolidation: full verification, launch screenshot pass, doc reconciliation, release notes.** *Verification from a clean state* (`rm -rf .next` + `pnpm install --frozen-lockfile`): `pnpm lint` (0 errors, 8 pre-existing warnings), `pnpm typecheck`, `pnpm test` **635 tests / 82 files**, `pnpm packages:build`, `pnpm packages:test` **61 tests** (9 `@usertrack/protocol` · 22 `@usertrack/node` · 30 `@usertrack/better-auth`), `pnpm build` — all green. `PORT=3100 pnpm start` + `scripts/cache-check.mjs` (16/16 routes: ISR `HIT` on `/`, `/categories`, `/rankings`, `/sitemap.xml`, `/s/<slug>`, `/u/<username>`; `public, s-maxage=300` on the search-param boards; never on `/sign-in` or `/api/account/export`) and curl checks on `/api/health`, `/api/health?deep=1` (`"convex":"ok"` + a `jobs` array), `/api/v1/leaderboard` (`x-ratelimit-limit: 60`), `/api/badge/<slug>.svg` (`frame-ancestors *` — badges stay embeddable while every other route is `DENY`) and `/leaderboard` (HSTS, CSP, `X-Frame-Options`, `Cache-Control: public, s-maxage=300, stale-while-revalidate=1800` on both requests). *`scripts/smoke.mjs` adapted to SEC-1*: sign-up now stops at "Check your inbox", so the script flips `emailVerified` through the Better Auth component adapter and signs in before onboarding (A209); the whole flow to publish + public page + dashboard passes again. *New `scripts/shots-ship1.mjs`* walks every launch surface signed-out (landing, `/leaderboard`, `/discover`, `/s/demo-northwind`, `/impressum`, `/privacy`, `/terms`, `/developers`, `/sign-up`, `/sign-in`, `/api/health?deep=1`, the check-inbox card and the unverified sign-in state) and signed-in (onboarding profile step, dashboard, the project form's TrustMRR row, Settings incl. connected accounts / Data & privacy / the type-DELETE dialog / Social with the X follower line, the rich public page with About + Company & stack, the same page in anonymous mode, `/stacks/nextjs`, `/u/<username>`) at 375 / 768 / 1440 into `docs/screenshots/v1/ship/` (A210). Serving the production build on a port other than the dev deployment's `SITE_URL` makes Better Auth answer `403`, so both scripts document the `npx convex env set SITE_URL http://localhost:3100` prerequisite (A208); and because `/s/[slug]` is an ISR route since OPS-2, the anonymous-mode pass renames the slug instead of reloading the same path, which would have served the pre-anonymous HTML from the route cache for another five minutes (A211). One real contract gap surfaced: `Saas.owner` in `src/lib/api/openapi.ts` never gained the `xFollowers` SOCIAL-1 added to the DTO and to `docs/API.md`, so the field is now declared (plus "absent in anonymous mode") and `openapi.test.ts` grew a "describes the v1.0 surface" case that asserts the PROFILE-1 fields, `owner.xFollowers`, `Profile.xFollowers*` and that no schema property is named `mrr` / `arr` / `profit` / `price` / `amount` — the one legacy `revenue` key must stay the deprecated, amount-free alias (635 tests). *Doc reconciliation*: 51 → **52 MCP tools** in `README.md`, `docs/ARCHITECTURE.md` (3 places) and the `server.test.ts` title; the ARCHITECTURE request diagram now shows `publicQuery` / `cachedQuery` with ISR instead of the `fetchQuery` OPS-2 removed; the analytics catalog is described as 44 client events; the README `docs/` listing gained the ten files it never mentioned plus `docs/screenshots/v1/`; `docs/ROADMAP.md` gained a **v1.0 — Launch hardening** section, four new known limitations and corrected "next opportunities" for envelope encryption and the board table; `docs/BACKLOG.md` marks SHIP-1 ☑ and adds a "zero lint warnings" leftover; `docs/CODECRAFT.md` records the model per loop (Fable 5.1 through SOCIAL-1, Opus 5 for OPS-1..SHIP-1 after the Fable usage limit hit mid-loop) and that phase 6 SHIP was deliberately not run. *New `docs/RELEASE-v1.0.md`*: what changed per ticket, the deploy order (Convex → migration → Railway) with the reason, the schema delta (3 tables, 37 indexes, optional fields only), rollback in four steps incl. resetting the Railway health-check path, the unmerged `waitlist` branch, and the residual-risk table (plaintext `integrations.config`, DoH TOCTOU, per-instance burst limiter, unverified TrustMRR shape, unlimited Better Auth sign-in, Sentry off without a DSN). *`HUMAN_TODO.md` rewritten around one ordered launch checklist*: 13 required steps (gateway secret → Resend → GitHub → X callback → TrustMRR → Rybbit goals incl. the new `waitlist_join` → Google consent screen → lawyer review → `convex deploy` → `migrations:nativeV1` → `railway up` + health-check path → domain/DNS in two phases (interim waitlist service, then the main app) → post-deploy checks) and six recommended ones, each with exact commands; the old standalone domain section collapses into a pointer. Also removed the two imports OPS-2 orphaned (`convex/gateway.ts`, `convex/boardScan.test.ts`). Not deployed and not pushed — by design.
- **OPS-3 — retention sweep, error tracking, error boundaries, health check, CI.** *Retention:* new `convex/retention.ts` — `retention.sweep`, one table per step, `take(200)` + reschedule (the `native.pruneEvents` pattern), scheduled last by the daily sweep and logged as the `retention sweep` `jobRuns` row: `syncRuns` 30 d · `webhookDeliveries` 30 d (`success` / `exhausted` only — a delivery that can still be retried stays) · `emailEvents` 180 d · `apiUsage` 90 d · `auditLogs` 365 d · `oauthStates` 1 d · `backfills` / `jobRuns` 90 d, and `snapshots` / `stageSnapshots` older than 180 days thinned to the last row of each UTC day (per stage), 10 projects and ≤ 400 expired rows per page. Nine new time indexes (`syncRuns.by_time`, `webhookDeliveries.by_status_created`, `emailEvents.by_created`, `apiUsage.by_day`, `auditLogs.by_time`, `oauthStates.by_created`, `backfills.by_time`, `jobRuns.by_time`, `stageSnapshots.by_saas_captured`). The periods are `RETENTION_DAYS` in `src/lib/legal.ts`, the same constant `/privacy` renders — its retention list now also names the OAuth state, the history-import log and the job log, and says "thinned to one sample per day" instead of "aggregated into daily rows". *Error tracking:* `@sentry/nextjs` behind `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_DSN` — no DSN, no `init` and no import (`src/instrumentation.ts` loads `sentry.server.config` / `sentry.edge.config` per `NEXT_RUNTIME`, `src/instrumentation-client.ts` code-splits the browser SDK, `next.config.ts` only wraps `withSentryConfig` when a DSN exists and uploads source maps only where `SENTRY_AUTH_TOKEN` is set). Errors only, no tracing / replay / PII; `src/lib/sentry.ts` drops request bodies and cookies, strips `authorization` / `x-api-key` / `cookie` / `x-ut-gateway-secret` and redacts `token` / `state` / `code` / `secret` parameters in URLs, query strings and breadcrumbs. CSP `connect-src` gained `https://*.ingest.de.sentry.io` (EU). *Error boundaries:* `src/app/error.tsx` and `src/app/global-error.tsx` (own `<html>` + font stack, "Try again" + "Go home", `digest` as reference, both report to Sentry when enabled); public pages degrade instead of crashing — `publicData()` in `src/lib/convex-public.ts` returns `null` when Convex is unreachable and the boards / landing page render `DegradedNotice` ("Live data is temporarily unavailable"), while the static OG cards and `sitemap.xml` fall back to `EMPTY_STATS` / their fixed URLs. *Health:* `GET /api/health` → `200 {ok, version, uptime}` with **no** Convex read (`railway.toml` `healthcheckPath` moved from `/leaderboard`), `?deep=1` adds one `public.stats` read plus the new gateway-only `jobs.health` (latest run per job) behind a 3 s timeout and answers `convex: "ok" | "down"` — still 200. *CI:* `.github/workflows/ci.yml` on push / PR (ignoring `apps/**` + markdown) runs lint, typecheck, test, the workspace packages and `pnpm build` with placeholder Convex URLs — no secrets, `convex/_generated` is committed; `release-packages.yml` untouched. *Docs:* A2 corrected (`better-auth` is pinned to **1.6.22**, the `useSession().data: never` typing is gone), ARCHITECTURE (retention table, error tracking / health / degraded reads, CSP), DEPLOYMENT (topology, Sentry + health env, health check, CI). 634 tests (`convex/retention.test.ts`, `src/lib/sentry.test.ts`, `src/app/api/health/route.test.ts`); screenshots `docs/screenshots/v1/OPS-3` via `scripts/shots-ops3.mjs`.
- **OPS-2 — every public page is cached and no public read walks the whole `saas` table.** *Caching:* the root layout no longer calls `getToken()` and `SiteHeader` no longer calls `isAuthenticated()` — the Dashboard / Sign-in switch moved into the client component `site/header-auth.tsx` (`authClient.useSession()`, fixed-width placeholder while pending), so nothing under `(public)` reads a cookie on the server any more. Public pages read Convex through `src/lib/convex-public.ts` instead of `convex/nextjs`' `fetchQuery`, which pins every request to `cache: "no-store"` and single-handedly forced every route dynamic: `publicQuery` (plain `ConvexHttpClient`, lazily constructed) for pages without search params and `cachedQuery` (`unstable_cache`, 300 s, key = function name + sorted args) for the ones with. `export const dynamic = "force-dynamic"` became `export const revalidate = 300` on every page under `src/app/(public)` and on `sitemap.ts`; `/s/[slug]`, `/s/[slug]/share/[kind]` and `/u/[username]` additionally export `generateStaticParams = async () => []`, without which Next 16 never registers a dynamic segment for on-demand ISR (`prerender-manifest.json → dynamicRoutes`). `src/lib/public-cache.ts` holds the policy (`PUBLIC_REVALIDATE` 300, `PUBLIC_CACHE_CONTROL` `public, s-maxage=300, stale-while-revalidate=1800`, the `CACHEABLE_PUBLIC` route list) and `next.config.ts` maps it onto those routes only — the SEC-1 security headers and CSP are separate entries and unchanged, `/app`, the auth pages and `/api/**` keep their own headers. `scripts/cache-check.mjs` proves it against a production build: `/`, `/categories`, `/rankings`, `/sitemap.xml`, `/s/<slug>` and `/u/<name>` answer the second request with `x-nextjs-cache: HIT`, the search-param boards carry the public `Cache-Control` per URL, `/sign-in` and `/api/account/export` never do. `hideFromSearch` pages still render (and still `noindex`) from cache. *Bounded reads:* the board definitions moved into `convex/lib/boardRules.ts` (`BOARD_RULES`: inclusion predicate, score, comparator and the `saas` index whose descending order **is** the board order); `sortBoard` is now filter → compare → slice over those rules and behaves exactly as before. `public.boardRows` walks that index and stops as soon as the page is full, extending the walk only while the indexed score ties with the last row on the page, so `/leaderboard?limit=100` reads ~100 documents instead of every public row; category and stack pages keep using `by_public_category` / the capped public set. 15 new `saas` indexes back the boards (`by_public_trending24h/7d/30d`, `by_public_growth24h/7d/30d`, `by_public_total`, `by_public_new24h/7d`, `by_public_activation_rate`, `by_public_rank_delta7d`, `by_public_signup_conv`, `by_public_trial_conv`, `by_public_converted_growth`, `by_public_verified_at`) plus the new materialized `saas.growth24hPct` (written by `recomputeDerived` and every rerank). New singleton table `publicStats` (`key: "public"`) carries the directory counters — total / verified products, tracked users, 30-day new users, per-category and per-stack counts and last-sync stamps — written by `leaderboard.writePublicStats` from the page walk `rerank` already does, so `public.stats`, `public.boardMeta` and the `/discover` category chips are O(1) instead of a full scan (they fall back to a live count when the row does not exist yet). `publicSet` is capped at 5,000 rows ordered by 30-day new users and only serves what genuinely needs a set (search tags, the `most-activated` board, `boardMeta` for category × stack); the discovery feed resolves one project per referenced milestone / event instead of loading the public set; "Related products" reads its own category index plus the strongest 300 rows; `public.sitemap` takes 5,000 products off the index and reports `hasMore`; `gateway.publicRows` is gone — MCP `trending`, `discover` and the datasets endpoint go through `boardRows`. `convex/boardScan.test.ts` asserts the indexed board equals the in-memory sort for **every** board × window × limit × verifiedOnly and for category / platform / size / stack filters, `discovery.test.ts` does the same for all `/discover` sections; read-cost figures in `docs/ARCHITECTURE.md` → Public caching. 634 tests, screenshots `docs/screenshots/v1/OPS-2`.
- **OPS-1 — every scheduled job is paged; no cron reads or writes a whole table in one transaction.** New `convex/jobs.ts`: the `PAGE` catalog (projects per transaction), `startRun` / `recordPage` / `jobError` helpers and the `jobs.begin` / `jobs.record` internal mutations for action drivers, plus the `jobRuns` table (`job`, `startedAt`, `finishedAt`, `pages`, `items`, `errors`, `lastError`, index `by_job_time`) so an operator can see every run in the Convex dashboard. Two shapes, applied consistently: **paged mutation drivers** (`internalMutation({ cursor?, runId? })` → read one page → process → `recordPage` → re-schedule with `continueCursor`) for `daily.run` (**10** projects per page — each reads up to 400 `dailyMetrics` + all its milestones), `trust.dailyReview` (**50**), `daily.standings` (**50**), `share.benchmarkSweep` (**100**), `email.lifecycle.noGrowthSweep` (**100**), `digest.generate` and `email.reports.generateMonthly` (**50** profiles, already paged, now with run records); and **two-phase action drivers** where the result needs a global ordering: `leaderboard.rerank` (phase 1 `rankInputs` pages `{ id, slug, eligible, newUsers30d, growth30dPct, totalUsers, trending scores }`, phase 2 computes leaderboard + 24h/7d/30d trending ranks in memory, phase 3 `applyRanks` writes patches, rank history, milestones, rank-jump events and webhooks in pages of 50) and `daily.benchmarks` (phase 1 `benchmarkInputs` pages cohort keys + metric values, phase 2 `writeAggregates` in chunks of 100, phase 3 `daily.standings` re-reads the stored deciles per product). `daily.snapshotRankings` became an action that pages the public rankable set (**200**) and writes one `rankingSnapshots` row per board/category through `writeSnapshot`; `integrations.listAll` became `integrations.pageAll` (**500** ids per page) so `sync.runAll` builds the fan-out page by page and keeps the identical 10-minute stagger (scheduling in chunks of 100); `cohorts.projectsWithLinks` pages (**200**) and `cohorts.rebuildAll` keeps its 2 s stagger across pages. Failure isolation: every per-project body is wrapped in `try/catch` — the error is logged with the project id and counted on the run, the page and the job continue. The weekly digest no longer collects the whole public table per page: trending comes from the new `saas.by_public_trending` index, movers from the top **200** of the new `saas.by_public_rank` index, followed products are resolved by id and follows are capped at **500** per profile. No ranking / trending / milestone / benchmark semantics changed — `convex/jobs.test.ts` (5 tests) reranks 120 products across pages and compares against a single-pass sort, recomputes the `all` growth deciles, drives the whole daily pipeline over **250** projects through `finishAllScheduledFunctions` (asserting `pages === ceil(items / PAGE)` for every driver), stages the sync fan-out + cohort rebuild, and proves a failing project only costs its own row. `internal.leaderboard.rerank`, `internal.daily.benchmarks` and `internal.daily.snapshotRankings` are now actions (`tx.action` in the existing tests, `npx convex run` unchanged); cron names are unchanged. Docs: ARCHITECTURE "Background jobs" (page sizes + ceilings), DEPLOYMENT verify step, BACKLOG Epic 95, ASSUMPTIONS A186–A191.
- **SOCIAL-1 — X follower counts for connected founders (OAuth, free tier).** `profiles.xFollowers?` / `xFollowersAt?` (schema, optional) are read **only** from the founder's own X token — `GET /2/users/me?user.fields=…,public_metrics` (`X_ME_URL` gained `public_metrics`, `followersOf()` in `convex/lib/xApi.ts`); there is no lookup by handle (paid tier). Sources: `social.completeOAuth` → `storeConnection { followers }` on Connect X, `authProfile.applyProviderProfile` on X sign-in / account link (`fetchProviderHandle` returns `xFollowers`; the count always replaces the stored one while handle / avatar still only fill empty fields; prefills carry it until `profiles.upsert` creates the profile). Daily refresh: `social.refreshFollowers` (internal action, scheduled from `daily.run` +35 s) pages active `socialConnections` in pages of 50, chains itself through the scheduler, refreshes the token when `needsRefresh`, patches the profile, tolerates 429 (ends the run, tomorrow catches up) and 401 (connection `status: error` + `lastError`, same as the posting job), never throws for one founder; no-op without `X_CLIENT_ID`. `social.refreshNow` (Settings → "Refresh now", 1/min per founder via `xFollowersAt`; throws `ConvexError` so the cooldown / reconnect reason reaches the toast). `social.disconnect` forgets the count with the tokens. Shown on `/u/<username>` (`𝕏 12.4k followers on X`), the owner chip of leaderboard rows and the "Built by" block on `/s/<slug>` (`𝕏 12.4k`), the founder OG image / share-card chip (`@ada · 12.4k followers`), Settings → Social (count, refreshed-ago, Refresh now; typed-handle founders see "Connect X below to show your follower count"). `publicProfile` carries `xFollowers` / `xFollowersAt`, so `profilePublic = false` (404) and anonymous projects (`owner: null`) never leak it; API `Profile.xFollowers?` + `xFollowersAt?` and `Saas.owner.xFollowers?` (`dto.ts`, OpenAPI), MCP `usertrack_get_profile` gains `profile.xFollowers`. Analytics `x_followers_refreshed`. Tests: `convex/social.test.ts` (completeOAuth stores followers, missing metrics leave it, disconnect clears, paging over 51 connections, 401 / 429 / OAuth-off handling, token renewal, refreshNow cooldown, public exposure + anonymous stripping + hidden profile, sign-in import + prefill), `profileDto` / owner DTO tests. Docs: PROFILES, SOCIAL, API, MCP, ANALYTICS; screenshots `docs/screenshots/v1/SOCIAL-1/`.
- **IMPORT-1 — "Import from TrustMRR" prefill (URL or slug, operator key).** `convex/lib/trustmrr.ts`: `parseTrustmrrRef` (full `trustmrr.com/startup(s)/<slug>` URLs with scheme / `www` / trailing slash / query, or a bare `^[a-z0-9-]{1,80}$` slug), tolerant `mapTrustmrrStartup` (`data` wrapper or bare object, camelCase or snake_case, arrays of objects / strings or comma strings) → `TrustmrrPrefill` (name, description, website, logo, category + markets via `TOPIC_ALIASES`, tech stack via squashed slug / label match + aliases + `vuejs`⇄`vue` suffix tolerance, marketing channels via aliases + category fallback, cofounders with canonical X handles, ISO country, funding, team-size buckets `11-25|26-50 → 11-50`, `51+ → 50+`, founded month, value proposition / problem / audience / pricing model, `projectType: mobile` for `mobile-apps` / RevenueCat / Superwall) plus `unmapped[]` (`stack: …`, `channel: …`, `market: …`); every revenue / price / growth / traction field is ignored by construction (test asserts no such key). `fetchStartup` (10 s timeout, key only in the header, 404 / 429 / 401 / 5xx / timeout → typed `TrustmrrError`). `convex/trustmrr.ts`: `status` query (`configured`), `takeSlot` (rate limiter: `trustmrrImport` 5 / 10 min per profile + `trustmrrGlobal` 10 / min for the operator key), `importStartup` action (founder path, `ConvexError { code, message }`), `TRUSTMRR_API_KEY=fixture` serves the documented example offline. Schema: `saas.trustmrrSlug` (validated, clearable, hidden in anonymous mode, "Also on TrustMRR" link on `/s/[slug]`). UI: `TrustmrrImport` next to the Product title on the new / edit forms — inline URL row, loading, error, diff-style preview ("keeps yours" / "overwrites", unmatched values listed, "Overwrite existing values"), Apply fills the empty fields and highlights them (form remounts with the captured values), Linked / Unlink line. MCP `usertrack_import_from_trustmrr` (52 tools; `projects:write`; preview only, or `apply` / `overwrite` with a project ref through `updateProject` + audit `import_from_trustmrr`); gateway codes `not_configured` (501) and `upstream` (502). Analytics `trustmrr_import_started` / `_succeeded {unmappedCount}` / `_failed {reason}`. Tests: `convex/lib/trustmrr.test.ts` (parsing, 3 fixtures, revenue exclusion, limits, fetch errors), `convex/trustmrr.test.ts` (not configured, bad input, success, 404 / 429, per-founder limit, MCP preview / apply / overwrite / audit, anonymous stripping, slug validation). Screenshots `docs/screenshots/v1/IMPORT-1/`.
- **PROFILE-1 — Rich SaaS profile: markets, tech stack, marketing channels, cofounders, company + product details, anonymous mode, hide from Google, logo upload.** Schema (`saas`, all optional): `markets[]` (≤5), `techStack[]` (≤20, curated `src/lib/tech-stack.ts` with simple-icons slugs + free text), `marketingChannels[]` (≤15), `cofounders[]` (≤5 × name / X / GitHub), `country` (ISO-2, `src/lib/countries.ts`), `funding`, `teamSize`, `valueProposition` / `problemSolved` / `audience` / `pricingSummary` / `additionalInfo`, `anonymous`, `hideFromSearch`, `logoStorageId`; `name` ≤100, `description` ≤500. Validation in `convex/domain/projects.ts` (curated slugs, caps, handle normalisation, country check) shared by `saas.update`, MCP `usertrack_update_project` (every field, `foundedAt` as `YYYY-MM`) and REST DTO / OpenAPI (`anonymous`, `cofounders`, `markets`, `techStack`, `marketingChannels`, `company`, `about`). Anonymous mode enforced in `domain/visibility.ts` (`ANONYMOUS_HIDDEN`, `publicLogo`) + `public.ts` (`publicOwner`, `founderRows`): owner, cofounders, logo, website and store links vanish from page, OG, share cards, boards, feed, watchlists, frozen rankings and the API; name + metrics stay. `hideFromSearch`: `noindex, follow` on `/s/[slug]` + share pages, excluded from the sitemap (boards / API unchanged). Logo upload via Convex storage (`saas.generateLogoUploadUrl`, ≤1 MB png / jpg / webp, SVG refused, old file deleted on replace / remove / project delete). Form (`saas-form.tsx`) restructured into Product (logo Upload / Use URL, counters), Markets / Tech stack (searchable, icons) / Channels chip selects (`chip-select.tsx`), Founders (profile X handle read-only + cofounder rows), Company details, Product details, Additional settings (slug, Anonymous, Hide from Google); works at 375 px. Public page: About panel, Company & stack panel with `StackChip`s, cofounders under Built by, anonymous card. `/stacks/[slug]` SEO board ("SaaS built with Next.js", sitemap) and `?stack=` on `/discover` + boards (`public.board` / `boardMeta` / `discover`). Tests: `convex/domain/projects.test.ts`, `convex/profile.test.ts` (update / MCP / anonymous surfaces / sitemap / stack filters / logo upload), visibility + DTO anonymous cases. Docs: `docs/PROFILES.md` → Product profile, API + MCP tables. Screenshots `docs/screenshots/v1/PROFILE-1/`.
- **AUTH-1 — GitHub + X sign-in, connected accounts, profile import.** `convex/lib/authProviders.ts` builds Better Auth `socialProviders` only from the credentials that exist (`GOOGLE_*`, `GITHUB_*`, and the same `X_CLIENT_ID` / `X_CLIENT_SECRET` as Connect X — provider id `twitter`, callback `/api/auth/callback/twitter`), a strict X `getUserInfo` (Better Auth 1.6.22 would store the username as email when X shares none; now the sign-up fails with `email_not_found` and `/sign-in` explains what to do), `account.accountLinking` (trusted google / github / twitter, `allowDifferentEmails` for explicit links) and the `auth.providers` query so the UI disables buttons that are not enabled on the deployment. Profile import: `account.onCreate` trigger → `authProfile.importProviderProfile` reads the provider token through the component adapter, fetches the GitHub login / X handle + avatar once and fills empty profile fields (`applyProviderProfile`); before the profile exists the values wait in the new `profilePrefills` table (`profiles.me.prefill` prefills the onboarding form, `profiles.upsert` consumes it, account purge removes it). UI: "Continue with GitHub" / "Continue with X" next to Google (`src/components/auth/provider-icons.tsx`, analytics `sign_in` / `sign_up_started` with method `github` / `x`), Settings → **Connected accounts** (`connected-accounts.tsx`: `listAccounts`, `linkSocial`, `unlinkAccount`, the last sign-in method cannot be disconnected, email & password status). Privacy policy names the three providers. Tests: `convex/lib/authProviders.test.ts`, `convex/authProfile.test.ts`, purge coverage for `profilePrefills`. `HUMAN_TODO.md`: GitHub OAuth App, X callback + email permission.
- **LEGAL-1 — Impressum, Privacy Policy, Terms, footer, consent-free analytics notice.** New pages `/impressum` (German, mirrors thecodecave.de/impressum: Anbieter · Vertretung · Inhaltlich verantwortlich · Streitbeilegung + Haftung für Inhalte / Links · Urheberrecht), `/privacy` (English with a binding German summary block — controller, contact, data-subject rights —, 14 anchored sections: data categories derived from `convex/schema.ts`, purposes + legal bases table, processors/third countries with SCCs, retention numbers from `RETENTION_DAYS`, strictly-necessary-cookies-only → no banner, self-hosted cookieless Rybbit, public-by-design, rights incl. LDI NRW, self-service deletion/export at `/app/settings`, 16+) and `/terms` (English, German law, § 521 BGB-style liability, Köln venue for merchants, CC BY 4.0 for public growth data, “Data under review”, acceptable use, no SLA); `/imprint` → `/impressum` (301). `src/lib/legal.ts` holds operator data, `EFFECTIVE_DATE`, `RETENTION_DAYS`, `LEGAL_PAGES`, supervisory authority; `src/components/site/legal-page.tsx` renders prose-width legal text with a section index. New `src/components/site/footer.tsx` (`SiteFooter`, full + `compact`) replaces the inline public footer and is rendered in the auth layout and the app shell (legal · developers · API · MCP · webhooks · GitHub packages · ©). Sign-up form shows “By creating an account you agree to the Terms and acknowledge the Privacy Policy”. Sitemap lists the three pages (yearly, 0.3); root layout emits `<link rel="privacy-policy">` / `<link rel="terms-of-service">`. `docs/DATASETS.md` gains the CC BY 4.0 licence line. Tests: `src/lib/legal.test.ts`. Smoke: `docs/screenshots/v1/LEGAL-1/`.
- **LEGAL-2 — GDPR account deletion (Art. 17) + data export (Art. 20).** `convex/account.ts`: `exportAccount` / `buildExport` (account, profile, projects with settings, integrations through `publicConfig` only, milestones, follows, webhook endpoints with masked secrets, developer tokens masked, email preferences, X connection summary — never a credential), served by the session-authenticated `GET /api/account/export` (`Content-Disposition: attachment; filename="usertrack-export.json"`, `private, no-store`) and the new read-only MCP tool `usertrack_export_account` (`profile:read`; 51 tools). `deleteAccount` (requires `confirm: "DELETE"`) unpublishes every project and the founder profile, revokes tokens and disables webhooks in the first transaction, then schedules `account.purge` (sends the new transactional **account-deleted** email while the address still exists, revokes the X token best-effort) → `purgeStep`, a self-rescheduling internal mutation that deletes at most `PURGE_BUDGET` (400) rows per transaction across all 32 user-owned tables (projects via the new shared `removeProjectRows` / `removeSaas` in `convex/domain/projects.ts` — now also used by `saas.remove` and `seed.ts`, and covering integrationEvents, backfills, shareEvents, embedSites, rankHistory, benchmarkHistory and emailEvents that the old dashboard delete missed), fixes follower counters, and only then removes the profile and the Better Auth sessions / accounts / verifications / user through the component adapter (`deleteAuthUser` in `convex/email/users.ts`). Demo products are skipped; `shareStats`, `benchmarkAggregates` and frozen `rankingSnapshots` hold no personal data and stay. Settings gains a **Data & privacy** panel (`/app/settings#data-privacy`: Download my data, Delete account dialog listing what goes, type DELETE, sign-out, hard navigation to `/` with a one-shot `FlashToast` (`sessionStorage`, mounted in the public layout — a client-side push raced the app shell's sign-in redirect). `revokeXToken` extracted from `social.disconnect`. Tests: `convex/account.test.ts` (paged purge across every table with a second founder and demo data untouched, export never contains a secret), MCP counts 51, template render. Docs: ARCHITECTURE deletion section, MCP.md, README, privacy policy links `/app/settings#data-privacy`. Screenshots: `docs/screenshots/v1/LEGAL-2/`.
- **SEC-3 — SSRF protection for every provider fetch.** New `convex/lib/ssrf.ts` (`checkPublicHttpsUrl`, `isBlockedHost`, `isPrivateIp` with decimal / hex / octal / shorthand IPv4 normalization via `canonicalIpv4`, `allPublic`, `resolvePublicHost` over Cloudflare DoH, own-deployment hosts from `SITE_URL` / `CONVEX_SITE_URL` / `CONVEX_CLOUD_URL`); `convex/lib/webhooks.ts` and `convex/webhooks.ts` now wrap it (behaviour unchanged). Providers validate founder-supplied hosts (`endpoint.url`, `native.url`, `posthog.host`, `plausible.host`, `supabase.url`, `auth0.domain`: https only, no userinfo, no internal names, no private IP literals, ports 443 / 8443 except endpoint / native) and expose `hosts(config)`; `convex/providerRun.ts` resolves them right before every sync / verify / backfill and refuses private answers with one generic non-retryable `blocked: private or internal address` error (also for stored pre-SEC-3 configs). `fetchJson`, Supabase API mode and the native pull use `redirect: "manual"` (3xx → *Endpoint redirected — point the URL at the final location*) and a 15 s `AbortSignal.timeout`. `convex/node/postgres.ts` resolves the database host with `dns.lookup` before `client.connect()` (`assertPublicDbHost`, mapped by `explain()`, `UT_ALLOW_PRIVATE_DB=1` for local dev). Native base URLs no longer accept `http://localhost`. Tests: `lib/ssrf.test.ts`, `providers/ssrf.test.ts`, `providerRun.test.ts`, `node/postgres.guard.test.ts`. Docs: `docs/PROVIDERS.md` → Security, `docs/ARCHITECTURE.md`, `docs/WEBHOOKS.md`.
- **SEC-2 — real rate limiting (Convex-backed, trusted client IP).** `src/lib/client-ip.ts` (`clientIp`) keys every anonymous limit on the **last** `x-forwarded-for` hop (the one Railway appends) instead of the client-controlled first one, validated as an IP literal, falling back to `x-real-ip` then `unknown`. New Convex component `@convex-dev/rate-limiter` (`convex/convex.config.ts`) with the catalog in `convex/lib/rateLimits.ts` (`anonApi` 60/min per IP, `apiKey` 120/min per key, `mcp` 120/min per token — `PLANS.free.mcp.burstPerMinute` 60 → 120 —, `badge` 120/min, `embed` 60/min shared by iframe + JSON (was 120), `card` 40/min, `nativeEvents` 600/min per project, `xCallback` 10 per 10 min) and one gateway-gated mutation `rateLimits.check { name, key } → { ok, retryAfterMs, remaining }`. `src/lib/api/rate-limit.ts` gains `limit(req, name, key?)` — in-memory `take()` first, Convex second, degrades to local on Convex errors — and `tooMany()`; every former `take` caller (`withApi`, badge, embed, embed JSON, both card routes, X callback, native events, MCP tool calls) uses it and answers `429` + `Retry-After` + `Cache-Control: no-store`; `X-RateLimit-Remaining` now comes from Convex. Better Auth sign-in is not limited here (see A128). Tests: `client-ip.test.ts`, `rate-limit.test.ts`, `convex/rateLimits.test.ts` (component registered in convex-test), respond / MCP tests stub the limiter. Smoke: `docs/screenshots/v1/SEC-2/`.
- **SEC-1 — gateway fail-closed, honest credential wording, open redirects, security headers, email verification.** `convex/lib/gateway.ts` (`requireGateway` / `gatewayMatches`, constant-time compare) now rejects every gateway, embed-record and native-event call when `UT_GATEWAY_SECRET` is missing or different — it is required in dev too (README setup). Every "encrypted at rest" claim about provider credentials became "stored server-side, never returned to the dashboard, the API or an agent" (dashboard, `/developers`, MCP tool descriptions, setup plans, docs); envelope encryption is a backlog item. `src/lib/safe-redirect.ts` (`safeInternalPath`) validates `next` in the auth form and `redirectTo` in the X OAuth connect / callback routes and in `social.beginOAuth`. `next.config.ts` sets HSTS, nosniff, Referrer-Policy, Permissions-Policy, CSP (`frame-ancestors 'none'`) and `X-Frame-Options: DENY` everywhere except `/embed/*`, `/api/badge/*`, `/api/embed/*`, `/widget.js` (`frame-ancestors *`). Better Auth `requireEmailVerification: true`: sign-up shows "Check your inbox", unverified sign-in shows an inline notice with "Resend verification email", and publishing a project (dashboard, onboarding, MCP), a public founder profile or completing onboarding requires a verified email (`forbidden: Verify your email to publish`). Tests: `convex/lib/gateway.test.ts`, `src/lib/safe-redirect.test.ts`, `convex/publishGate.test.ts`, gateway / embed tests updated. Note: `scripts/smoke.mjs`, `scripts/ui-shots.mjs`, `scripts/shots-*.mjs` sign up throwaway accounts and now stop at the inbox state unless the account is verified first.
- **ANALYTICS-1 — Rybbit: script, typed event catalog, identify, server-side events, goal list.** Self-hosted cookieless Rybbit on every page: `<AnalyticsScript/>` (`next/script`, `data-site-id`, skip `/api/**` `/embed/**` `/mcp`, mask `/email/preferences*` `/reset-password*`; host + site id via `NEXT_PUBLIC_RYBBIT_HOST` / `NEXT_PUBLIC_RYBBIT_SITE_ID`, empty id or `NODE_ENV=test` disables it; CSP reads the same host). `src/lib/analytics.ts`: typed `Events` catalog (43 browser events, `EVENTS` value kept in sync via `satisfies`), `track()` (no-op without `window.rybbit`, booleans stringified), `identify()` / `reset()` replayed after script load; `<AnalyticsIdentity/>` links the pseudonymous Better Auth user id and clears it on sign-out. Every catalog event wired at its real call site (landing CTAs via `CtaLink`, board filters, search, compare, share studio + share page, badge / embed / MCP snippet copies, dataset links on `/developers`, sign-up / sign-in / resend / sign-out, onboarding steps + completion, project create / update / publish, integration open / test / connect / remove / sync / backfill, Postgres wizard, X connect / disconnect, follow / unfollow, tokens, webhooks, notification prefs, export, delete). Server-side: `serverTrack()` (`after()`, `POST /api/track`, path only, 2 s timeout, optional `RYBBIT_API_KEY`) for `api_request` (withApi, incl. 401 / 429), `mcp_tool_called`, `badge_rendered`, `embed_rendered`, `native_event_ingested`; `convex/lib/analytics.ts` (opt-in via `RYBBIT_SITE_ID` on the deployment) for `webhook_delivered` and `sync_completed`. Privacy policy §7 / §8 now describe events, autocapture, web vitals, errors, masked paths and the pseudonymous id in local storage. `docs/ANALYTICS.md` (catalog, site settings, goals + funnels table), `scripts/analytics-check.mjs` (playwright network log). Tests: `src/lib/analytics.test.ts`, `src/lib/analytics-server.test.ts`, `convex/lib/analytics.test.ts`. Smoke: `docs/screenshots/v1/ANALYTICS-1/`.

## 0.9.0 — Discovery, follow, history, benchmarks v2, datasets, webhooks (2026-09-03)
- **Discovery** (`docs/DISCOVERY.md`): `/discover` narrows to a category (`?category=`), new sections Fastest growing this month, Popular mobile apps; Biggest movers now read the **stored position 7 days ago** (`rankHistory`) — "#78 → #31 · 7d" — instead of the previous 4-hour refresh; Hidden gems and Biggest movers are boards (`/hidden-gems`, `/biggest-movers`) with the rules on the page; New & rising requires ≥ 5 new users this week; platform filter (web / mobile / hybrid) on every board; cards show new users **and** growth %, plus a follow chip; feed gains `rank_jump`, `traction` and `benchmark` events (stored, keyed, never synthesized); related products on project pages; search returns categories.
- **Follow + watchlists** (`docs/FOLLOWS.md`): idempotent `follows.follow` / `unfollow` (self-follow, private targets rejected, cap 500), `follows.ids` for chips, `/app/following` rebuilt as a personalized feed (milestones, spikes, launches as "new from a founder you follow", rank jumps, weekly rank changes ≥ 5 places, benchmark months, traction), 7 / 30 / 90-day selector; email sub-preferences `followedMilestones` / `followedRanking` / `followedSpikes` under the existing master switch; `GET /api/v1/following` (API key) and MCP follow / watchlist tools.
- **History** (`docs/HISTORY.md`): append-only `rankHistory` (leaderboard + trending per window and day) written by every rerank, `rank7dAgo` / `rankDelta7d` / `trendingRank7dAgo` / `bestTrendingRank` materialized; weekly `benchmarkHistory`; monthly `rankingSnapshots` (frozen on the 1st); `backfills` provenance rows, idempotent `recordHistory` (a day is never imported twice), `backfilledAt` only after success, owner-triggered "Backfill history" (≤ 90 days) with a run list; `public.history` with resolution-aware downsampling (raw / day / week / month) and explicit `gaps`; the growth chart uses a real time axis and shades gaps instead of drawing through them; rank history chart on project pages; `GET /api/v1/saas/{slug}/rank-history`, `/benchmark-history`, `/history` returns `{ resolution, points, gaps }`.
- **Benchmarks v2** (`docs/BENCHMARKS.md`): cohorts `cat`, `cat|size`, `size`, `platform`, `age` (founder-entered founding month, new optional project field) or `tracked` (tracking age), `all`; new metric growth acceleration; `MIN_SAMPLE` 5 → **10**; cards carry cohort definitions, bands (Top 5 / 10 / 20 / 25 %), previous percentile and change sentences ("improved from the 55th to the 70th percentile since last month"), 26-week history; public statement gated by the new "Benchmark statement" visibility toggle (default on); monthly `benchmark` feed events at ≥ 90 %.
- **Public datasets + SEO** (`docs/DATASETS.md`): new pages `/fastest-growing-developer-tools`, `/fastest-growing-mobile-apps`, `/best-activation-rate-saas`, `/best-converting-mobile-apps`, `/hidden-gems`, `/biggest-movers`, `/rankings` + `/rankings/<year>/<month>/<category>`; every ranking page has an intro, methodology panel, last-updated stamp, JSON-LD `ItemList`, internal links; redirects `/trending-saas`, `/new-and-rising`; sitemap covers the new pages and frozen rankings; `GET /api/v1/datasets/{trending,fastest-growing,new-and-rising,hidden-gems,movers,categories/{slug},rankings/history}` with `window` / `category` / `platform` / `limit` / `cursor` / `format=json|csv`.
- **Webhooks** (`docs/WEBHOOKS.md`): endpoints with one-time `whsec_` secrets, 7 event types (`milestone.reached`, `rank.changed`, `trending.rank_changed`, `growth.spike`, `integration.failed`, `integration.recovered`, `project.verified`) + `webhook.test`; versioned payloads; HMAC-SHA256 `UserTrack-Signature` / `-Timestamp` / `-Event` / `-Delivery` / `-Event-Id` headers; async delivery with 10 s timeout and retries 5 min / 30 min / 2 h / 12 h, delivery ledger, auto-disable after 25 failures, hourly retry sweep; SSRF policy (HTTPS only, private / metadata / internal hosts refused, DoH re-resolution before every delivery); `/app/developer/webhooks` (create, events, project scope, test, deliveries, retry, rotate, disable, delete) and public docs `/developers/webhooks`; MCP `usertrack_get_webhooks` / `create` / `update` / `test` / `get_webhook_deliveries`.
- **Platform**: scopes `follows:read` / `follows:write` / `webhooks:read` / `webhooks:write`; MCP 36 → 50 tools (`usertrack_discover`, follow / unfollow project & founder, `usertrack_get_watchlist`, `usertrack_get_rank_history`, `usertrack_get_benchmark_history`, `usertrack_get_dataset`, webhook tools); OpenAPI 3.1 updated; `webhook retry sweep` cron; docs set (`DISCOVERY`, `FOLLOWS`, `HISTORY`, `BENCHMARKS`, `DATASETS`, `WEBHOOKS`, `API`, `MCP`, `ARCHITECTURE`, `ASSUMPTIONS`, `ROADMAP`).
- **Fixes**: `recordHistory` could insert duplicate backfilled snapshots on a reconnect; `markBackfilled` ran even when the provider errored; history charts compressed sync outages on a category axis; `dailyMetrics.rank` had no reader (kept for the monthly report; `rankHistory` is canonical).

## 0.8.0 — Founder identity & sharing (2026-09-03)
- **Founder profiles** (`/u/<username>`, `docs/PROFILES.md`): header with avatar, name, @username, X handle (+ `connected` chip only after OAuth), bio, website / GitHub / LinkedIn / location / joined; founder-level aggregates over public projects (`convex/lib/founder.ts`: Σ users, Σ new users 7d/30d, combined growth %, change vs previous 30d, **weighted** activation rate, published converted users, best rank, trending count, biggest-growth project); aggregate growth chart (`public.founderHistory`, forward-filled per project, Total / New / per-project breakdown); project grid (logo, category, rank, trending, users, sparkline, 30d); `Person` JSON-LD, `"{Founder} — SaaS Founder on UserTrack"` metadata, `noindex` without public products; `profiles.profilePublic` hides everything. Search shows founders with project count, users and 30-day growth.
- **X handles** (`src/lib/social.ts`): `@name` / `name` / x.com URL → canonical handle, validated (1–15 `[A-Za-z0-9_]`), optional in onboarding (profile step now also has the avatar URL), `/app/profile`, `/app/settings/social`, MCP. States `unavailable` · `handle_provided` · `connected_via_oauth`; a typed handle is never shown as verified.
- **Share Card Studio** (`src/components/share/*`, `docs/SHARING.md`): live preview, presets **Blueprint / Aurora / Minimal**, 1200×630 + 1080×1080, timeframe for graph cards (7d–all) with an honest chart (real min→max scale printed as MAX / MIN + first / last date), toggles (chart, logo, founder handle, verification line, date range), custom title; Download PNG, Copy image (ClipboardItem with download fallback), Copy link, Post to X. Share buttons on Total users / New users 7d + 30d / growth chart / rank & trending chip / activation / conversion (public only) / benchmark statement / milestones / SaaS share pages / founder header + aggregate chart / dashboard cards. Card config lives in the URL (`src/lib/share-card.ts`), so PNGs are deterministic and cacheable; card routes are rate-limited (40/min/IP). New kinds: `benchmark` (public top-quarter statement) and the founder card (`/u/<username>/card`, also the profile OG image). "Verified by UserTrack" only for verified sources, otherwise "Tracked on UserTrack".
- **Share engine + Share Center** (`convex/share.ts`, `convex/lib/shareRules.ts`, `/app/share`): `shareEvents` created exactly once per significant achievement — user milestones ≥ 100 (activated ≥ 1K, converted ≥ 100), Top 100 / Top 10 / best rank / Top 10 trending, best day/week ≥ 100, 30- and 90-day streaks, +25/50/100 % months, ≥ 3× spikes (per day), top-10 % benchmarks (one per metric per month via the daily `share.benchmarkSweep`); scored so agents and the Center show the strongest first; Ready / Shared / Dismissed with restore; per-event Studio, Copy link, Post to X. Demo products never create events. Anonymous `shareStats` counters (generated / downloaded / copied / X). Milestone emails link to the Share Center. Dev helper `seed:simulateGrowth`.
- **X sharing & drafts** (`src/lib/x-drafts.ts`): rotating, data-driven templates per event kind (≤ 280 chars incl. URL), founder- vs UserTrack-authored voices, verification wording only for verified data, founder tagging only when allowed; `xIntentUrl` everywhere.
- **X OAuth + auto-posting (feature-flagged)** (`convex/social.ts`, `convex/lib/xApi.ts`, `docs/SOCIAL.md`): OAuth 2.0 PKCE via `/api/social/x/connect` → `/api/social/x/callback` with profile-bound single-use `oauthStates` (10 min), token exchange + `users/me` in a Convex action, `socialConnections` (tokens server-only), avatar/handle import on connect, refresh a minute before expiry, disconnect with best-effort revoke; hourly `social.autoPost`: opt-in per category (all **off** by default), ≤ 1 post / founder / day, one attempt per event, secret-free errors surfaced in settings, no retries. **UserTrack account pathway** with OAuth 1.0a env credentials (`oauth1Header`, pinned by X's documented vector): verified-only, 1K+ users / Top 10 / best rank ≤ 3, ≤ 3 posts/day, founder opt-outs for promotion and tagging. `/app/settings/social` UI. Flags `X_CLIENT_ID`/`X_CLIENT_SECRET`, `X_BOT_*` — human steps in `HUMAN_TODO.md`.
- **Public API**: `/users/{username}` now returns `location`, `xState`, `joinedAt`, card / history URLs and `metrics` (founder aggregates); new `/users/{username}/history?range=`; OpenAPI updated. **MCP** (36 tools): `usertrack_get_profile`, `usertrack_update_profile` (new scope `profile:write`), `usertrack_get_share_events`, `usertrack_create_share_card`, `usertrack_get_x_draft`, `usertrack_get_founder_url`, share workflow in the server instructions.
- Schema: `profiles.location / profilePublic / xUserId / xConnectedAt / socialPrefs`, tables `shareEvents`, `socialConnections`, `socialPosts`, `oauthStates`, `shareStats`. Crons: hourly `social.autoPost`; daily sweep schedules `share.benchmarkSweep`.
- Tests: 387 in 44 files (+24 on top of v0.7): X handles, drafts, card config, share rules, founder aggregates + history, X API builders + OAuth 1.0a vector, share engine (dedupe, floors, spikes, benchmarks, stats), founder queries, MCP tool set.
## 0.7.0 — Native SDK integrations (2026-09-03)
- **One provider kind `native`** replaces `better_auth`: any client that speaks the native protocol is a verified users / activation / conversion source. `config.source` names the adapter (`better-auth` · `prisma` · `drizzle` · `convex` · `authjs` · `custom`); `better_auth` stays accepted as an input alias (API / MCP / detection) and existing rows are rewritten by `migrations:nativeV1` (integrations, snapshots, stage snapshots, sync runs). Trust base 40, "Verified via Prisma (UserTrack SDK)" provenance, per-source default base path (`/api/auth` vs `/api/usertrack`), per-source 404 wording and minimum client version.
- **`@usertrack/protocol` 0.1.0** (`packages/protocol`): the v1 signing scheme extracted (WebCrypto only, zero deps), wire types with the additive `users` / `activation` / `conversion` / `identities` blocks, `source` + `clientVersion` (`pluginVersion` / `provider` kept as deprecated aliases), five lifecycle event types, `EVENTS_PATH` `/api/integrations/native/events` + `LEGACY_EVENTS_PATH`. Fixtures moved here (two new cases); `convex/lib/nativeProtocol.ts` is the self-contained server twin, pinned by the fixtures and a randomised cross-implementation test.
- **`@usertrack/node` 0.1.0** (`packages/node`): `createUserTrackHandler({ projectId, secret, users, activation?, conversion?, identities?, source?, debug? })` → WHATWG fetch handler (Next.js route export, Hono, Remix, Bun, Cloudflare) + `toNodeHandler()` (Express / `node:http`); count sources are `{ count({ createdAtGte?, createdAtLt? }) }` with `exact: false` and `timeFilter: false` escape hatches; `createTracker().track(type, { id, at? })` fire-and-forget push. Adapters: `@usertrack/node/prisma` (`prismaUsers`, `userTrackPrismaExtension` push hook on `create` / `delete`), `/drizzle` (`drizzleUsers`, `select count(*)` with time bounds, optional peer), `/convex` (`convexHandler` for `httpAction`, `countWithCap` over `by_creation_time` with `exactCounts: false` beyond 10k), `/authjs` (`userTrackAuthjsEvents` for `events.createUser`; counting through the Prisma / Drizzle user model, `createdAt` caveat documented). 22 tests + HTTP e2e (`packages/node/e2e`), README, `AGENTS.md` / `llms.txt`, CHANGELOG, HUMAN_TODO.
- **`@usertrack/better-auth` 0.2.0**: thin wrapper over the two packages (`betterAuthUsers` count source + `handleMetrics` core + shared tracker); emits the nested `users` block, `source: "better-auth"`, `clientVersion`; events go to the native path (UserTrack keeps accepting the old one). 30 tests, e2e green. `AGENTS.md` / `llms.txt` added.
- **Server**: `convex/providers/native.ts` (parses both shapes, `describe()` follows `config.reported`, `fetchHistory` for users + activation), `convex/native.ts` (`createNativeIntegration({ url?, source, rotate? })` — rotation re-keys attached rows; `ingestEvent` verifies the native or legacy path and stores `user.created` / `user.deleted` / `user.activated` / `trial.started` / `user.converted`; event summary per type), `convex/lib/nativeSetup.ts` (per-source install plan: package, route / plugin snippet, optional push hook, notes, rules), `ProviderMetrics.reported` + `domain/integrations.recordReported` (auto-attaches activation / conversion sibling rows sharing the credential — never replacing an explicitly connected source), `providerLabel(kind, config)` for source-aware labels everywhere, `normalizeProviderKind`.
- **Catalog / recommendation**: `native` entry with `source` credential; aliases for Better Auth, Auth.js / NextAuth, Convex, Prisma, Drizzle, Lucia, Kysely, Mongoose, Sequelize, TypeORM, MySQL, SQLite, custom → `native` (the endpoint stays the fallback when nothing is detected); `detectNativeSource`; strong native signals (Better Auth, Auth.js, Convex) rank first, ORM-only signals after hosted auth providers; `nativeSource` / `composition.users.source` in the recommendation; activation + conversion suggested from the same handler; `conversionSetup({ provider: "native" })`.
- **Next / MCP / dashboard**: events route moved to `/api/integrations/native/events` (`/api/integrations/better-auth/events` re-exports it for 0.1.x plugins); `usertrack_get_native_setup { source }` (30 tools), `usertrack_get_better_auth_setup` kept as a deprecated alias, `usertrack_create_integration { provider: "native" | "better_auth", source? }`, prompt `add_native_sdk_project_to_usertrack`, workflow + server instructions updated; "My app (SDK)" card with a source picker in the connect wizard (Create → Install with per-source snippet + optional push hook → Verify → Live), live events line shows activations / trials / conversions, onboarding stack answers Auth.js / Convex / Custom map to native; public docs `/developers/integrations/native` with per-source tabs (`/developers/integrations/better-auth` links to it).
- Release workflow `.github/workflows/release-packages.yml` covers `protocol-v*`, `node-v*`, `better-auth-v*`; root `pnpm packages:*` scripts cover all three packages (61 tests). Docs: `README.md`, `docs/PROVIDERS.md`, `docs/ARCHITECTURE.md`, `docs/MCP.md`, `docs/API.md`, `docs/ASSUMPTIONS.md` (A77–A88), `docs/ROADMAP.md`, `docs/BACKLOG.md` (epics 56–59), root + package `HUMAN_TODO.md`.

## 0.7.0 — Embeddable growth widgets (2026-09-03)
- **Live widgets** (`public/widget.js`, `src/app/embed/[slug]/route.ts`, `src/lib/widget.ts`): one `<script async src=…/widget.js data-slug data-type data-theme data-window>` inserts an iframe (`/embed/<slug>`) with four types — `users` (live user count with count-up), `growth` (growth % · window), `verified` (Verified by UserTrack, trust label + shield), `chart` (320×120 mini chart with sparkline, delta, synced-ago) — theme `auto` (`prefers-color-scheme`) / `dark` / `light`, transparent body, size reported to the host via `postMessage`, refresh every 5 minutes while visible, hover affordance, `not found` pill for drafts / unknown slugs (404). Every widget links to `/s/<slug>?ref=embed&utm_source=embed&utm_medium=widget&utm_campaign=<type>`; branding always shown. `no-store`, `noindex`, 120 req/min per IP.
- **`GET /api/embed/{slug}.json`** (`public.widget`): public numbers only (visibility applied), 60 s cache, CORS. `next.config.ts` caches `/widget.js` for an hour.
- **Distribution tracking**: table `embedSites` (saasId, host, loads, firstSeenAt, lastSeenAt) + `saas.embedSiteCount`; `embeds.record` (gateway secret, `normalizeHost`: lowercase, no `www.` / port, hostname regex) is called from the embed route in `after()` with the `Referer` host — own host and `localhost` ignored, one write per host and project per minute. `saas.getMine.embedSites[]` (last 20 by last seen).
- **Configurator** (`/app/saas/[id]/embed`): Live widget (type, timeframe, theme; live iframe preview; Script / iframe / JSON snippets) and SVG badge modes, “Where it's embedded” list (host, loads, since, last seen; empty state explains the rules). Manage page `#embeds`: “Embedded on N sites: …”; overview / manage next action “Add the widget to your site”. Public page: “Embedded on N sites” chip next to the badge.
- **MCP**: `usertrack_get_embed_code` gains `format: "badge" | "widget"` and `theme: "auto"`; widget results carry `script`, `iframe`, `iframeSrc`, `jsonUrl`, `width`, `height`. `projectUrls.widget`.
- **Shared** `src/lib/embed.ts` (`parseWidgetParams`, `widgetQuery`, `widgetSnippets`, `widgetPageUrl`, sizes) used by the route, the configurator and the gateway. Tests: `embed.test.ts`, `widget.test.ts`, `convex/embeds.test.ts` (26 new assertions across 10 tests). Script `scripts/shots-widgets.mjs`.
- Docs: README, `docs/API.md` (Widgets section), `docs/ARCHITECTURE.md`, `docs/MCP.md`, `docs/ROADMAP.md`, `docs/BACKLOG.md` (Epic 60).

## 0.6.0 — Better Auth native integration (2026-09-02)
- **`@usertrack/better-auth`** (`packages/better-auth`, MIT, ESM, peer `better-auth >=1.3`): `userTrack({ projectId, secret, endpoint?, events?, debug? })` adds a signed `POST /usertrack/metrics` endpoint (total users, 24h/7d/30d signups, ≤90-day daily series, arbitrary ranges; anonymous-plugin users excluded; `adapter.count` with bounded scan fallback; 30 req/min) and fire-and-forget `user.created` / `user.deleted` events with an HMAC-derived subject. Never blocks or fails the host auth flow; no PII. 42 unit tests + a real-HTTP e2e sample (`packages/better-auth/e2e`). README, CHANGELOG, `HUMAN_TODO.md` (npm scope, trusted publishing, first publish, community listing), release workflow `.github/workflows/release-better-auth.yml`.
- **Protocol v1** (HMAC-SHA256, ±5 min, nonce-bound responses) implemented in the package and in `convex/lib/betterAuthProtocol.ts`, frozen by shared fixtures.
- **Provider `better_auth`** (`convex/providers/betterAuth.ts`): native verified users source (trust base 40), signed pull + response verification, protocol/plugin version gate, actionable secret-free errors (404 plugin missing, 401 env mismatch, stale clock, 5xx, network), 30-day `newUsers` history in one request. Registered in schema, registry, catalog (Better Auth first in `USERS_PRIORITY`; aliases `better-auth`, `@better-auth/*`, `@convex-dev/better-auth`, `BETTER_AUTH_*`), UI catalog, MCP enum, stack recommendation.
- **Credentials** (`convex/betterAuth.ts`): `ut_int_` secret (238 bits) generated by UserTrack, shown once, stored in `integrations.config` with SHA-256 + prefix, `rotate`; integration starts `awaitingVerification` (skipped by cron / Sync now); `integrations.verifyStored` + `markVerified` turn the first successful verify into the first sync. Schema: `integrations.awaitingVerification / verifiedAt / pluginVersion / protocolVersion / lastEventAt`, new table `integrationEvents`.
- **Events**: `POST /api/integrations/better-auth/events` (Next, per-project rate limit) → `betterAuth.ingestEvent` (gateway secret, HMAC verify, dedupe by `eventId`) → `eventSummary` ("Live events: n signups since last sync"); pruned after 30 days in the daily sweep.
- **Dashboard**: Better Auth card first ("Plugin · 2 min"), 4-step wizard (Create → Install with one-time env reveal + npm/pnpm/yarn/bun tabs + config snippet → Verify with live result card → Live), "Waiting for deployment" status row with rotate + verify, plugin version on the connected row.
- **AI onboarding**: extra "Plugin installed & deployed" step for Better Auth projects; provider labels instead of ids.
- **MCP** (29 tools): `usertrack_get_better_auth_setup` (package-manager-aware install plan, config example, env, `.env.example`, code-modification safety rules, version check), `usertrack_create_integration` (credential once, idempotent, rotate, audit `create_integration`), `usertrack_verify_integration` marks pending native integrations connected, `usertrack_get_provider_recommendation` lists Better Auth first with `nativePlugins`, `usertrack_get_integration_setup { provider: "better_auth" }` returns the same steps, prompt `add_better_auth_project_to_usertrack`, workflow + server instructions updated.
- **Docs**: public page `/developers/integrations/better-auth`; `README.md`, `docs/PROVIDERS.md`, `docs/MCP.md`, `docs/ARCHITECTURE.md`, `docs/ASSUMPTIONS.md` (A70–A76), `docs/ROADMAP.md`, `docs/API.md`, `docs/BACKLOG.md` (epics 50–55).
- Workspace: `pnpm-workspace.yaml` → `packages/*`; root tsconfig/eslint exclude `packages`; scripts `packages:build|test|typecheck`.

## 0.5.0 — Lifecycle model: Growth → Activation → Conversion (2026-09-02)
- **Normalized lifecycle** Reached → Signed up → Activated → Trial → Converted (`convex/domain/funnel.ts` v3): dynamic partial funnels, flows for every stage, strategic rates (`rates[]`), per-stage provenance + freshness + health, `basis` / `identityQuality`, `funnelHistory` (trailing-7-day ratios). Stage keys renamed (`visitors → reached`, `signups → signed_up`, `paying → converted`). `docs/FUNNEL.md`.
- **Provider roles = lifecycle sources**: `conversion` role (legacy `revenue` migrated), capabilities `trial` · `converted` · `identity`, `ProviderMetrics` conversion fields + `identities`, `normalizeRole`, `ROLE_STAGE`.
- **Conversion providers** (read-only, amount-free): Stripe rewritten (subscriptions by status, conversion modes, `metadata.userId` identities, no MRR/prices/expand), **RevenueCat** (`metrics/overview`: active trials + active subscriptions; customers are never registered users), **Paddle**, **Lemon Squeezy**, **Chargebee**, endpoint (`convertedUsers`, `trialUsers`, `newConverted*`, `newTrials*`, `mode`, `identities`). Shared `convex/providers/conversion.ts` (`SubRecord`, `aggregateConversion`, bounded paging).
- **Sync engine**: conversion branch (stage snapshots, daily `convertedUsers` / `newConverted` / `trialUsers` / `newTrials`, windows, `convertedGrowth30dPct`, `signupToConvertedPct` / `activatedToConvertedPct` / `trialToConvertedPct` via `stageRates` on every stage sync, `converted` milestones), identity ingestion (salted SHA-256 in the action, batches of 500), `stageSnapshots` for activation too.
- **Identity & cohorts**: `identityLinks` + `cohortMetrics` tables, `convex/lib/identity.ts` (hashing, quality rules, cohort maths), `convex/cohorts.ts` (daily paged rebuild, purge on source replacement, owner/public views), identity quality `aggregate_only` · `partially_mapped` · `cohort_verified`, Cohort Verified badge. `docs/IDENTITY.md`.
- **Visibility model** (`convex/domain/visibility.ts`): per-metric public keys (totalUsers, growth, activationRate, conversionRate, trialConversion, convertedCount, traffic), `saas.setVisibility`, `stripPrivate` applied to every public projection; connection ≠ publication; conversion private by default.
- **Project type** `web` · `mobile` · `hybrid`, `appStoreUrl` / `playStoreUrl` (store-domain validated), `authMethods` (Sign in with Apple / Google … are methods, never a user source).
- **Migration** `migrations:lifecycleV1`: role rename, Stripe `mode`, legacy toggles → `visibility`, `payingUsers` → `convertedUsers`, `mrr` / `currency` cleared on `saas` and `dailyMetrics`. `IDENTITY_SALT` env on dev + prod.
- **Dashboard IA** (`/app/saas/[id]`): three metric groups **Growth** (Total / New users, growth %, velocity) · **Engagement** (Activated users, activation rate, retention) · **Conversion** (Trial users when reliable, Converted users, Signup → Converted, Activated → Converted, Trial → Converted), each with its own source health chip (Healthy / Needs attention / Stale) and freshness ("Updated 18m ago") so one broken provider never breaks the page; new **Visibility** section (per-metric public switches, "Connection ≠ publication" copy); Cohort Verified badge; no revenue tab, no MRR anywhere.
- **Funnel UI** (`src/components/public/funnel.tsx`): dynamic stages, 7D / 30D / 90D, conversion between stages, strategic rates, per-stage provenance + freshness, `null` counts rendered as rate-only, empty-state per missing stage; **funnel history chart** (`funnel-history-chart.tsx`, Recharts) with metric selection (Signup → Activated / Signup → Converted / Activated → Converted / Trial → Converted), one line by default.
- **Public profile** (`/s/[slug]`): Growth / Engagement / Conversion sections (conversion only when published), Cohort Verified badge, App Store / Google Play chips for mobile / hybrid products, funnel + history, share buttons for activation / conversion cards.
- **Onboarding**: "What are you tracking?" (Web SaaS / Mobile App / Both, `platform-picker.tsx`) → stack questions per platform (`stack-questions.tsx`, `src/lib/stack-recommendation.ts`: identity · analytics · monetization + auth methods for mobile) → recommended provider composition; payment setup never mandatory; "UserTrack never needs your revenue numbers" copy; `/app/saas/new` gets the same platform step; mobile layout (top-center toasts, skip in header).
- **Boards**: secondary leaderboards `best-conversion` (Signup → Converted), `best-trial-conversion`, `converted-growth` (`/best-conversion`, filters, sitemap) — only products that publish the metric; the core growth boards are unchanged and stay the default.
- **Benchmarks**: metrics `signupToConvertedPct`, `activatedToConvertedPct`, `trialToConvertedPct`, `convertedGrowth30dPct` with `basis: aggregate` labels (never mixed with cohort figures), `MIN_SAMPLE` unchanged; insights such as "Your signup-to-converted rate is 1.6× the median for SaaS with 1k–5k users".
- **Trending Score**: optional `conversion` factor `1 + 0.10 · clamp(signupToConverted, 0, 25) / 25` (max ×1.10) — a supporting signal only; trending stays about verified new users, velocity, acceleration, activation and trust (`docs/TRENDING.md`).
- **Milestones**: `converted` threshold milestones (converted-user counts), `convertedUsers` metric; no revenue milestones.
- **Emails**: monthly report per project adds activated + activation rate, converted users, Signup → Converted / Trial → Converted and *major funnel changes* (activation ±5 pts, conversion ±2 pts, `FUNNEL_CHANGE_POINTS`); conversion appears in the owner's private report even when private on the public page. No amounts.
- **Share cards**: `activation` ("68 % activation rate") and `conversion` ("12.4 % Signup → Converted") kinds, offered only when the metric is public; no revenue cards.
- **Public API v1** (additive): `GET /saas/{slug}/funnel` v3 (stage keys `reached` / `signed_up` / `activated` / `trial` / `converted`, `value: null` for rate-only publication, `verified`, `health`, `updatedAt`, `rates[]`, `basis`, `identityQuality`), new `GET /saas/{slug}/conversion`, `GET /saas/{slug}/engagement`, `GET /saas/{slug}/cohorts`; `metrics.conversion`, `identityQuality`, `projectType`, `stores` on the SaaS object; `metrics.revenue` kept as a deprecated `{ payingUsers }` alias without amounts; conversion boards in `/leaderboard`; OpenAPI + `docs/API.md` + `/developers`.
- **MCP** (27 tools): `usertrack_get_provider_recommendation` takes `projectType` / `detectedAuth` / `detectedAnalytics` / `detectedPayments` and returns the lifecycle composition; new `usertrack_get_conversion_setup`, `usertrack_get_identity_mapping`, `usertrack_get_funnel_history`, `usertrack_get_cohorts`; `usertrack_get_funnel` v3; `usertrack_get_integration_setup` / `configure` / `verify` accept `role: "conversion"` for all five payment providers; 12-step workflow; prompt `add_mobile_app_to_usertrack`; server instructions: "conversion state only, never revenue". `docs/MCP.md`.
- **Sync pipeline**: users → activation → conversion → lifecycle aggregation (`stageRates`) → funnel history → benchmarks → trending; per-role cadence unchanged (4 h), daily cohort rebuild in the daily sweep.
- Tests: conversion aggregation + all five providers, identity hashing / quality / cohorts, visibility gating, lifecycle funnel (full, partial, hidden counts, health, history).

## 0.4.0 — Growth data layer (2026-09-02)
- Provider layer v3 (`convex/providers/types.ts`): per-configuration **capability model** (`describe()` → `totalUsers` · `createdUsers` · `historicalUsers` · `activationEvents` · `retention` · `traffic` · `revenue`), **source verification levels** (`verified` / `partially_verified` / `self_reported` via `verificationLevel()`), `runtime()` + `toPostgres()` for TCP sources, `fetchJson` backoff on 429/503 (Retry-After, ≤5 s, ≤2 retries), `mapLimit` for bounded backfills. `convex/providerRun.ts` is the only V8-vs-Node switch; nothing else branches on provider kind. Docs: `docs/PROVIDERS.md`.
- **PostgreSQL provider** (`convex/providers/postgres.ts` + `convex/node/postgres.ts`, `"use node"`, `pg` as external package): read-only session (`SET default_transaction_read_only = on`), 10 s connect / 20 s statement timeouts, aggregate SQL only (`count(*)` with optional created-at / soft-delete / status filters, `GROUP BY day` history), identifiers validated + quoted, epoch columns supported, custom `$1` SELECT for activation (validated: single SELECT/WITH, no `;`, no write keywords), secret-free error mapping (`explain()`), verified trust. Setup wizard (`postgres-wizard.tsx`): connect + SSL → ranked table list (≤200, row estimates) → column suggestions + preview count → live test → connect; SQL template for a `usertrack_ro` role in the UI and in MCP setup instructions.
- **Supabase read-only database mode** (`mode: "database"`, session-pooler hosts only): `auth.users` with `created_at` / `deleted_at` → verified 24h/7d/30d signups + 30-day history in one query; API mode (service role key) kept as fallback. Mode is derived from the config (connection string present → database). Runs through the same Node runtime.
- **Firebase signup scan**: `accounts:batchGet` pages of 1,000 keeping only `createdAt` (≤ `FIREBASE_SCAN_LIMIT` = 100k accounts) → real 24h/7d/30d windows + 30-day history; larger projects fall back to totals + snapshot deltas; `scanSignups: false` opt-out. **Clerk**: 4 concurrent history requests, 429 backoff. Recommendation order `supabase → clerk → firebase → auth0 → postgres → endpoint` with new detection aliases (`DATABASE_URL`, `pg`, `prisma:postgresql`, `drizzle-pg`, Neon, Vercel Postgres, …).
- Live **"Test connection"** card (`integrations.test`): validates, fetches once, shows detected counts, source verification level, masked config and the capability list before anything is stored. Connected sources show verification + capabilities on the manage page.
- **Activation + funnel v2** (`convex/domain/funnel.ts`): Visitors → Signups → Activated → Paying over `7d` / `30d` / `90d` from stored `dailyMetrics` rows (never snapshots), previous window of the same length, per-stage `changePct` / `conversionPct` / `previousConversionPct`, flow vs stock stages, fallbacks to materialized metrics while history is short, **per-stage provenance** (`source { provider, label, verification }`) and a funnel-level `verification` (`verified` only when every stage is). Owner view includes private traffic/revenue; public view only opted-in stages. Onboarding gained an optional activation step. Docs: `docs/METRICS.md`.
- **Trending Score v2** (`convex/lib/trending.ts`): `100 · volume · growth · acceleration · trust · activation · freshness · history`; `freshness` = 1 up to 24 h since the last sync, linear to 0.5 at 72 h, 0 after; `history` = `0.6 + 0.4 · min(1, trackedDays/14)`; no signal (score 0) below 5 new users, for stale sources or under review. Per-window ranks `trendingRank24h` / `trendingRank` (7d) / `trendingRank30d` with `prev*` = position at the previous rerank; deterministic order (`score → newUsers30d → totalUsers → slug`). `public.trendingExplain` + ⓘ tooltip and one-line `explain` on trending cards. Docs: `docs/TRENDING.md`.
- **Discovery feed from stored events** (`public.feed`, `feedItems`): milestones + `spike` / `activation_spike` / `launched` / `verified` events merged and deduplicated by stable ids (`milestone:{saasId}:{key}`, `{kind}:{saasId}:{day}`); `launched` written once at first publish (`saas.launchedAt`), `verified` once at the first verified users sync (`saas.verifiedAt`); `events.by_time` index. `/discover` v2: Trending now, Fastest today / this week, New & rising, Recently verified, Biggest movers (7-day trending rank gains), Hidden gems with public rules (`< 1,000` users, ≥ 10 new / ≥ 10 % in 7d, ≥ 7 days history, trust ≥ 60), Top developer tools, Top AI, category counts, activity feed.
- **Share engine v2** (`src/lib/share.ts`, `src/lib/og/share-card.tsx`): card kinds `users` · `growth` (30d) · `week` (7d) · `rank` · `trending` · `activation` · `milestone-<id>` · `spike-<id>`; `/s/[slug]/share/[kind]` page, `opengraph-image` and `/card` PNG (1200×630, `?size=square` → 1080×1080) from one renderer; `Cache-Control: public, max-age=300, s-maxage=3600, stale-while-revalidate=86400`; growth spikes get their own share card.
- **Badges / embeds v2** (`src/lib/badge.ts`, `/api/badge/[slug].svg`): types `users` · `growth` · `trending` · `verified` · **`chart`** (320×120 live widget: total, window delta, 30-day sparkline), `theme=dark|light`, `window=7d|30d` (growth, chart), `compact=1` (drops the word, keeps the mark; chart 320×96); 120 req/min per IP burst limit (429 + `Retry-After`), CORS `*`, `nosniff`, edge cache 1 h, neutral "not found" SVG with 404 for drafts/unknown slugs. New configurator page `/app/saas/[id]/embed` (type, timeframe, theme, size, live preview, HTML / Markdown / image URL).
- **Benchmarks v2** (`convex/lib/benchmarks.ts`, `daily.benchmarks`): cohorts `all` / `cat:<slug>` / `size:<bucket>` over rankable products, five metrics (`growth30dPct`, `newUsers30d`, `activationRatePct`, `growth7dPct`, `trendingScore7d`), `MIN_SAMPLE = 5` (smaller cohorts are deleted), deciles only, percentile rounded to steps of 5 in `5 … 95`, `medianMultiple`, insight sentences, public statement only for the top quarter (`public.benchmarkHighlight`, category → size → all) shown on the product page. Docs: `docs/BENCHMARKS.md`.
- **Compare v2**: `/compare?s=a,b,c,d&days=7|30|90|365|all` (default 30) reading daily rows only, chart modes Total / Indexed (= 100 at each product's first day in the window), window-growth row, share buttons, OG image route `/compare/og?s=…&days=…` (1200×630, cached 1 h).
- **Public API v1** additions: `GET /saas/{slug}/funnel?timeframe=`, `GET /saas/{slug}/benchmarks`, `GET /discover?category=&limit=` (sections + deduplicated feed), `GET /compare?s=&days=` (absolute + indexed series); new DTOs, OpenAPI updated. Docs: `docs/API.md`.
- **MCP**: 8 new tools (23 total) — `usertrack_get_provider_recommendation`, `usertrack_get_activation_setup`, `usertrack_get_funnel`, `usertrack_get_trending`, `usertrack_get_benchmark`, `usertrack_compare_projects`, `usertrack_get_share_card`, `usertrack_get_embed_code`; setup workflow is now 10 steps (provider recommendation with the Supabase → Clerk → Firebase → PostgreSQL → endpoint priority, optional activation step); `postgres` accepted by the integration tools. Docs: `docs/MCP.md`.
- Onboarding + dashboard IA: manual onboarding has 5 steps (Profile → Your SaaS → Data source → Activation (optional, skippable) → Publish) with a richer publish preview; overview page shows state-derived "Next actions" (publish, connect activation, add badge, share card, see how you compare) and compact benchmark cards; manage page sections with anchors (`#overview` `#growth` `#funnel` `#benchmarks` `#integrations` `#sharing` `#embeds` `#settings`) and a "Next steps" row.
- Tests: 262 (28 files) — adds funnel, discovery/feed (`convex-test`), Postgres SQL builders + error mapping, Clerk backoff, Firebase pagination + scan cap, benchmarks, share copy/kinds, badge v2, new DTOs, MCP tool registry.

## 0.3.0 — Public API keys & MCP (2026-09-02)
- Tagline: "the growth data layer for SaaS" — website, public API and MCP share one domain layer.
- Domain layer `convex/domain/{projects,integrations,metrics}.ts` with typed `DomainError`; `saas.ts` / `integrations.ts` and the new gateway delegate to it. Domain normalization (`convex/lib/domain.ts`) makes project creation idempotent by canonical domain.
- Developer tokens (`developerTokens`): API keys `ut_api_…` and MCP tokens `ut_mcp_…`, SHA-256 hashed, shown once, six scopes, optional expiry (≤365 d), revoke, max 25 active; per-day usage buckets (`apiUsage`); audit trail (`auditLogs`). `/app/developer` page with key/token creation, usage and activity.
- Token-authenticated Convex gateway (`convex/gateway.ts`): `UT_GATEWAY_SECRET` check, scopes, strict ownership by id or slug, daily quota, create ≤10/h, verify 20 s cooldown, sync 60 s cooldown, audit on every write, secrets never returned.
- Public API: optional API keys via `Authorization: Bearer` or `X-API-Key` (1,000 req/day + 120/min burst, uncached), `X-RateLimit-Window` and `X-RateLimit-Reset` headers, new endpoints `GET /saas/{slug}/metrics`, `GET /trending`, `GET /users/{username}`, error codes `unauthorized` / `revoked` / `expired` / `forbidden`, OpenAPI 3.1 document at `/api/openapi.json`.
- MCP server at `/mcp` (Streamable HTTP, stateless, JSON responses, `@modelcontextprotocol/sdk`): 15 tools (account, projects, create/update project, supported integrations, setup instructions, configure/verify/sync integration, metrics, history, rank, milestones, share URLs), server instructions with the 9-step setup workflow, prompt `add_project_to_usertrack`, structured `isError` results with hints, per-token burst + daily limits, discovery document on `GET /mcp`.
- Integration catalog and executable setup instructions (`convex/lib/integrationSetup.ts`): 10 providers with roles, trust, detection aliases, credential locations and env var hints, least-privilege permissions, security rules, endpoint code templates per framework/ORM, recommendation from detected stack.
- Onboarding "Set up with AI in 60 seconds": MCP token (7-day expiry), config snippets for Claude Code / Cursor / Codex CLI / VS Code / generic, agent prompt, live status derived from the audit trail (`convex/onboarding.ts`), funnel events; manual setup still available.
- `/developers` rewritten: overview, quickstart, API reference with examples/errors/limits, MCP reference (scopes, tools, workflow, limits, security, troubleshooting), examples. New `docs/MCP.md`; `docs/API.md`, `docs/ARCHITECTURE.md`, `docs/ASSUMPTIONS.md`, `docs/ROADMAP.md`, `README.md` updated.
- Tests: token format/hashing/scopes, domain normalization, integration setup.

## 0.3.0 — Email system (2026-09-02)
- Resend mailer behind one entry point (`convex/email/send.ts`): dedupe ledger (`emailEvents`), preference + suppression checks, retries with backoff, Resend idempotency keys, `List-Unsubscribe` one-click headers, delivery log with provider message ids.
- Typed, table-based HTML templates in the blueprint brand (dark graphite, white linework, pink accent), plain-text twins, XSS-safe escaping; every template unit-tested.
- Better Auth: welcome email (doubles as email verification for email+password signups, plain for Google), verification re-send from settings, password reset flow (`/forgot-password`, `/reset-password`).
- Lifecycle: profile unfinished after 24h, product without source after 24h (per-entity scheduled functions, once each), first-sync confirmation, source stopped syncing (6 consecutive failures or ≥3 with 24h silence, once per episode), source recovered.
- Growth: user milestones (10 → 1M, highest crossing mails, lower ones recorded), ranking milestones (Top 100/50/25/10/5/#1, board-size aware), spike alerts (≥2.5× the 30-day daily baseline, ≥20 users, 14-day history, 7-day cooldown), 7 quiet days (once per quiet period, healthy sources only), followed-product updates (opt-in, major events only).
- Monthly growth report: one consolidated email per founder for the completed calendar month (per project: start/end users, new, net, %, activation, rank start → end from daily rank history, biggest day, milestones; overall summary), generated on the 1st in pages of 50, delivered at 09:00 local time; in-app at `/app/reports`.
- Weekly digest rewired to the mailer and preferences (opt-in by default; silent when there is nothing to report).
- Preferences: `emailPreferences` per Better Auth user (product nudges, user / ranking milestones, growth alerts, monthly report, weekly digest, followed updates, timezone), `/app/settings/notifications`, signed 90-day preference links (`/email/preferences?token=…`), one-click unsubscribe endpoint, transactional mail never gated.
- Resend webhook (`/webhooks/resend`, Svix signature verified): delivered / bounced / complained / failed → event status; hard bounces and complaints suppress non-essential mail (`emailRecipients`).
- Google sign-in button + provider config (needs OAuth client, see `HUMAN_TODO.md`).
- Tests: 115 (rules, templates, tokens, webhook signatures, Resend adapter, and `convex-test` function tests for dedupe, preferences, lifecycle state machine, milestones, spikes, followers, monthly reports, webhook, token access).

## 0.2.0 — Trustworthy, discoverable, shareable (2026-09-02)
- Provider interface v2 (roles, capabilities, normalized metrics, history backfill); new adapters: Firebase Auth, Auth0, PostHog, Plausible, Google Analytics 4, Stripe; Clerk/Supabase/endpoint upgraded with range + active-user metrics.
- Sync engine v2: staggered cron, retries with backoff, run durations, last success/failure, previous-window deltas, multi-role integrations, one-time 30-day backfill.
- Activated users (24h/7d/30d, rate, chart series), estimated retention/churn, traffic + revenue with opt-in display, funnel.
- Trust score + anomaly flags (impossible growth, drops, reconnect churn, source switching, stale) with neutral public labels; under-review products unranked.
- Trending score (24h/7d/30d) with documented formula, trending rank + movement, trending milestones.
- Boards: trending, fastest, most users, most new, most activated, activation rate, new & rising; filters for window, category, size, verification.
- Automatic milestones (users/activated thresholds, best day/week, top 10/100, best rank, streaks, monthly growth) persisted once; spike detection; chart annotations.
- Share cards with OG images + downloadable PNG; SVG badge endpoint + copy-paste embed UI.
- Follow products/founders, following feed, weekly digest (in-app + Resend email when configured), digest opt-in.
- Categories, `/discover` (search + sections), `/trending`, `/fastest-growing-saas`, `/fastest-growing-ai-saas`, `/new-saas`, `/most-new-users`, `/categories/*`, `/compare`, `/developers`.
- Benchmarks (daily deciles per group) with percentile cards.
- Public API v1 (`/api/v1/*`) with stable DTOs, error envelope, CORS, rate limiting; `docs/API.md`.
- SEO: sitemap, robots, canonical URLs, JSON-LD, custom 404; mobile nav row.
- Dashboard overview, manage page with per-role sources, insights, sync log, share + embed; onboarding category + activation nudge; LinkedIn on profiles.
- Tests: 57 unit tests (metrics, trending, trust, milestones, spikes, retention, benchmarks, providers, DTOs, badge, rate limit); smoke test updated.

## 0.1.0 — MVP (2026-09-01)
- Next.js 16 App Router, Tailwind 4, shadcn/ui (Base UI), Convex, Better Auth (email + password) via `@convex-dev/better-auth`.
- Blueprint design system: graphite surfaces, white linework, pink `#FB0184` accent, Geist Sans/Mono, corner-tick panels, grid backgrounds.
- Auth: sign-up, sign-in, sign-out, session persistence, `proxy.ts` route protection.
- Profiles: handle availability check, display name, bio, links; public `/u/[username]`.
- SaaS: create/edit/delete, slug management, publish toggle, tags, logo URL; `/app/saas/*`.
- Onboarding wizard: profile → SaaS → data source → publish → share, resumable, mobile-first.
- Data sources: provider abstraction with Clerk, Supabase (auth users or table), JSON endpoint (verified on own domain), Manual (self-reported).
- Sync engine: append-only snapshots, 4-hour cron, derived metrics (24h/7d/30d, growth %), daily rollups, failure demotion to `pending`, manual "Sync now" with cooldown.
- Leaderboard: verified-only default, "all sources" toggle, ranking by 30-day verified new users, demo rows excluded from ranks.
- Public SaaS page: metric cards, Recharts growth chart (24H–ALL, total/new), provenance, founder card, share buttons.
- OG images for SaaS, profile and leaderboard (`next/og`, vendored Geist woff, brand wordmark).
- Brand assets: favicon / `icon.png` / `apple-icon.png` from the UT monogram, wordmark in header and OG frame (`public/brand`, sources in `brand/`).
- Demo seed (`seed:run` / `seed:clear`), Vitest unit tests, Playwright smoke test (`scripts/smoke.mjs`).
- Railway deployment (`railway.toml`), Convex production deployment, docs.
